{
  "setup": [
    "bun install",
    "[ -f ../../.claude/settings.local.json ] && mkdir -p .claude && cp ../../.claude/settings.local.json .claude/"
  ],
  "teardown": ["./scripts/dev.sh stop -k", "./scripts/clean.sh --all"],
  "test": ["bun test"],
  "ports": {
    "KANNA_DEV_PORT": 1420,
    "KANNA_RELAY_PORT": 9080,
    "KANNA_MOBILE_PORT": 1421
  }
}
