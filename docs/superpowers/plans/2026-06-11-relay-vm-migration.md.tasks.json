{
  "planPath": "docs/superpowers/plans/2026-06-11-relay-vm-migration.md",
  "tasks": [
    {
      "id": 9,
      "subject": "Task 1: Relay VM deploy stack files",
      "status": "pending",
      "description": "**Goal:** Add the Compose stack, Caddyfile, and on-VM deploy script that run the relay behind Caddy TLS.\n\n**Files:**\n- Create: `services/relay/deploy/compose.yaml`\n- Create: `services/relay/deploy/Caddyfile`\n- Create: `services/relay/deploy/vm-deploy.sh`\n\n**Acceptance Criteria:**\n- [ ] `docker compose -f services/relay/deploy/compose.yaml config` parses without error\n- [ ] `bash -n services/relay/deploy/vm-deploy.sh` exits 0\n- [ ] Caddyfile serves `relay.kanna.build` and proxies to `relay:8080`\n\n**Verify:** `docker compose -f services/relay/deploy/compose.yaml config --quiet && bash -n services/relay/deploy/vm-deploy.sh && echo OK` → `OK`\n\n**Steps:** Full code in plan doc Task 1.\n\n```json:metadata\n{\"files\": [\"services/relay/deploy/compose.yaml\", \"services/relay/deploy/Caddyfile\", \"services/relay/deploy/vm-deploy.sh\"], \"verifyCommand\": \"docker compose -f services/relay/deploy/compose.yaml config --quiet && bash -n services/relay/deploy/vm-deploy.sh && echo OK\", \"acceptanceCriteria\": [\"compose config parses\", \"vm-deploy.sh bash -n exits 0\", \"Caddyfile proxies relay.kanna.build to relay:8080\"]}\n```"
    },
    {
      "id": 10,
      "subject": "Task 2: Provision script for the relay VM",
      "status": "pending",
      "blockedBy": [9],
      "description": "**Goal:** One-time idempotent script that creates the service account, static IP, firewall rule, and e2-micro VM.\n\n**Files:**\n- Create: `services/relay/deploy/provision.sh`\n\n**Acceptance Criteria:**\n- [ ] `bash -n services/relay/deploy/provision.sh` exits 0\n- [ ] Script is idempotent (every `create` tolerates already-exists)\n- [ ] Script prints the static IP and the DNS instruction at the end\n\n**Verify:** `bash -n services/relay/deploy/provision.sh && echo OK` → `OK`\n\n**Steps:** Full code in plan doc Task 2. chmod +x provision.sh and vm-deploy.sh.\n\n```json:metadata\n{\"files\": [\"services/relay/deploy/provision.sh\"], \"verifyCommand\": \"bash -n services/relay/deploy/provision.sh && echo OK\", \"acceptanceCriteria\": [\"bash -n exits 0\", \"idempotent creates\", \"prints static IP and DNS instruction\"]}\n```"
    },
    {
      "id": 11,
      "subject": "Task 3: Retarget kd cloud deploy --relay to VM, error on staging, add provision command",
      "status": "pending",
      "description": "**Goal:** Production relay deploys go to the VM (ssh prep + scp + ssh run); staging relay deploys error; `kd cloud relay-provision` wraps provision.sh. TDD against the existing mock-runner test pattern.\n\n**Files:**\n- Modify: `tools/kd/src/runtime/cloud-deploy.ts` (`deployRelayCloud`, `RelayDeployResult`)\n- Modify: `tools/kd/src/tasks/registry.ts` (new `cloud.relay-provision` task near `cloud.deploy` ~line 729)\n- Test: `tools/kd/tests/cloud-deploy.test.ts`\n\n**Acceptance Criteria:**\n- [ ] production `deployRelayCloud` runs in order: relay build → gcloud builds submit (unchanged) → ssh `rm -rf ~/kanna-relay && mkdir -p ~/kanna-relay` → scp the three deploy files into `~/kanna-relay/` → ssh `sudo bash ~/kanna-relay/vm-deploy.sh`\n- [ ] staging `deployRelayCloud` rejects with \"staging relay is retired\"\n- [ ] result is `{projectId, vmName, zone, image, relayUrl: \"wss://relay.kanna.build\"}` (KANNA_RELAY_HOSTNAME/KANNA_RELAY_VM_NAME/KANNA_RELAY_VM_ZONE overrides)\n- [ ] `cloud.relay-provision` registry task executes `bash services/relay/deploy/provision.sh`\n- [ ] `cd tools/kd && pnpm test` passes; `pnpm exec tsc --noEmit` clean\n\n**Verify:** `cd tools/kd && pnpm exec tsc --noEmit && pnpm test -- cloud-deploy` → all pass\n\n**Steps:** TDD per plan doc Task 3 — tests first (exact call arrays), FAIL, implement, PASS, registry entry, full check.\n\n```json:metadata\n{\"files\": [\"tools/kd/src/runtime/cloud-deploy.ts\", \"tools/kd/src/tasks/registry.ts\", \"tools/kd/tests/cloud-deploy.test.ts\"], \"verifyCommand\": \"cd tools/kd && pnpm exec tsc --noEmit && pnpm test\", \"acceptanceCriteria\": [\"production deploy sequence: build, builds submit, ssh prep, scp three files, ssh vm-deploy\", \"staging relay deploy rejects with 'staging relay is retired'\", \"relayUrl wss://relay.kanna.build\", \"cloud.relay-provision registered\", \"kd tests + typecheck pass\"]}\n```"
    },
    {
      "id": 12,
      "subject": "Task 4: Point clients at wss://relay.kanna.build",
      "status": "pending",
      "description": "**Goal:** Replace the hardcoded Cloud Run relay URL in all three clients (and the Rust test asserting it).\n\n**Files:**\n- Modify: `apps/desktop/src-tauri/src/commands/mobile.rs:438` (`PRODUCTION_RELAY_URL`) and `:1390` (test assertion)\n- Modify: `apps/desktop/src/services/desktopRelayTerminal.ts:4` (`PRODUCTION_CLOUD_TRANSPORT_URL`)\n- Modify: `apps/mobile/src/appModel.ts:32` (`PRODUCTION_RELAY_URL`)\n\n**Acceptance Criteria:**\n- [ ] All three constants equal `wss://relay.kanna.build`\n- [ ] `grep -rn \"kanna-relay-402613185450\" apps/ crates/ packages/ tools/ | grep -v node_modules | grep -v .build` → no output\n- [ ] `cargo test relay_url` passes in apps/desktop/src-tauri\n- [ ] `pnpm exec tsc --noEmit` clean in apps/desktop and apps/mobile\n\n**Verify:** `cd apps/desktop/src-tauri && cargo test relay_url` → PASS; tsc clean in desktop + mobile\n\n**Steps:** TDD per plan doc Task 4 — Rust test assertion first, FAIL, update three constants, PASS + typechecks + grep.\n\n```json:metadata\n{\"files\": [\"apps/desktop/src-tauri/src/commands/mobile.rs\", \"apps/desktop/src/services/desktopRelayTerminal.ts\", \"apps/mobile/src/appModel.ts\"], \"verifyCommand\": \"cd apps/desktop/src-tauri && cargo test relay_url\", \"acceptanceCriteria\": [\"three constants equal wss://relay.kanna.build\", \"no kanna-relay-402613185450 references remain\", \"cargo test relay_url passes\", \"tsc clean in desktop and mobile\"]}\n```"
    },
    {
      "id": 13,
      "subject": "Task 5: Cutover — provision, DNS, deploy, end-to-end verification",
      "status": "pending",
      "blockedBy": [9, 10, 11, 12],
      "description": "**Goal:** Stand up the VM, serve the relay at `wss://relay.kanna.build`, and prove phone↔desktop works end to end before any teardown.\n\n**USER-ORDERED GATE — NON-SKIPPABLE.** This task was requested by the user in the current conversation. It MUST NOT be closed by walking around it, by declaring it \"verified inline\", or by substituting a cheaper check. Close only after every item in `acceptanceCriteria` has been re-validated independently, with output captured.\n\n**Files:** none (operational; uses scripts from Tasks 1–3)\n\n**Acceptance Criteria:**\n- [ ] `./kd cloud relay-provision` completes and prints the static IP\n- [ ] DNS A record `relay.kanna.build → <static IP>` added by the user and resolving (`dig +short relay.kanna.build` returns the IP)\n- [ ] `./kd cloud deploy --production --relay` completes\n- [ ] `curl -fsS https://relay.kanna.build/health` returns HTTP 200 with connection-count JSON\n- [ ] Desktop build with new URL connects (/health connection count increases; no 5s reconnect loop)\n- [ ] Phone↔desktop end-to-end: mobile pairs and a terminal roundtrip succeeds over the VM relay\n\n**Verify:** `curl -fsS https://relay.kanna.build/health` → HTTP 200 JSON, plus the manual phone↔desktop roundtrip\n\n**Steps:** (1) provision, capture IP. (2) USER ACTION: add A record, wait for dig. (3) deploy. (4) curl health. (5) ./kd dev up --mobile, pair, roundtrip; capture /health count before/after.\n\n```json:metadata\n{\"files\": [], \"verifyCommand\": \"curl -fsS https://relay.kanna.build/health\", \"acceptanceCriteria\": [\"provision completes and prints static IP\", \"dig +short relay.kanna.build returns the static IP\", \"kd cloud deploy --production --relay completes\", \"curl -fsS https://relay.kanna.build/health returns HTTP 200\", \"desktop connects: /health connection count increases\", \"phone-desktop terminal roundtrip succeeds over VM relay\"], \"userGate\": true, \"tags\": [\"user-gate\"], \"requireEvidenceTokens\": [[\"before-connect\", \"count=0\", \"provisioned\"], [\"after-connect\", \"connected\", \"roundtrip\"]]}\n```"
    },
    {
      "id": 14,
      "subject": "Task 6: Tear down the Cloud Run relay services",
      "status": "pending",
      "blockedBy": [13],
      "description": "**Goal:** Delete `kanna-relay` from both `kanna-build` and `kanna-staging` once the VM relay is verified, ending the always-on Cloud Run billing.\n\n**Files:** none (operational)\n\n**Acceptance Criteria:**\n- [ ] `gcloud run services delete kanna-relay --project kanna-staging --region us-central1 --quiet` succeeds\n- [ ] `gcloud run services delete kanna-relay --project kanna-build --region us-central1 --quiet` succeeds\n- [ ] `gcloud run services list` for each project no longer shows kanna-relay\n- [ ] Phone↔desktop still works after deletion (health check + pairing roundtrip)\n\n**Verify:** services lists show no kanna-relay rows; `curl -fsS https://relay.kanna.build/health` → 200\n\n**Steps:** Delete staging relay, delete production relay, confirm lists, re-check VM relay health.\n\n```json:metadata\n{\"files\": [], \"verifyCommand\": \"gcloud run services list --project kanna-build --region us-central1 && gcloud run services list --project kanna-staging --region us-central1 && curl -fsS https://relay.kanna.build/health\", \"acceptanceCriteria\": [\"staging kanna-relay deleted\", \"production kanna-relay deleted\", \"no kanna-relay rows in either project\", \"VM relay health still 200 after deletion\"]}\n```"
    }
  ],
  "lastUpdated": "2026-06-11"
}
