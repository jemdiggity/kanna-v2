# Task bb1b6988

Raise the default workflow `revision_limit` from 3 to 5, including explicitly pinned built-in workflows, documentation, prompt-facing expectations, and tests that assert the default.

Keep revision-budget semantics unchanged: agent-origin revisions consume rounds, human-origin revisions reset the count, exhaustion parks with the verdict, and existing pinned workflow snapshots are not migrated.

Reviewer directive (revision round 1, 2026-08-20): also update the bundled workflow-factory example and field description from 3 to 5, and pin that guidance with a core asset test. Complete when the focused asset test, `cargo test -p kanna-server`, `pnpm test`, and `./kd test all` pass.
