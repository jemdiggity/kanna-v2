# Task bb1b6988

Raise the default workflow `revision_limit` from 3 to 5, including explicitly pinned built-in workflows, documentation, prompt-facing expectations, and tests that assert the default.

Keep revision-budget semantics unchanged: agent-origin revisions consume rounds, human-origin revisions reset the count, exhaustion parks with the verdict, and existing pinned workflow snapshots are not migrated. Complete when `cargo test -p kanna-server` and `pnpm test` pass.
