# Workspace clippy gate

Remove every warning from `cargo clippy --workspace --all-targets` without changing runtime behavior, protocols, or schemas. Scope is Rust warning cleanup and the kd Rust gate, its tests, and testing documentation. Prefer code fixes; any necessary allow must be item-scoped with a reason. Async test serialization must not hold blocking mutex guards across awaits.

Done when strict workspace/all-target clippy, touched-crate tests, `./kd test all`, TypeScript checking, and Rust formatting pass. The canonical Rust lane must run strict clippy before test binaries and have a regression test. Regenerate the server lockfile if dependencies change.

Baseline started 2026-09-05 on clean task branch at fetched `origin/main` `cc0ac56073b272f717086292e9368e13c6425b25`. Before PR creation, rebase onto fetched origin/main (including in-flight tasks 756c4972 and 0108b94d) and repeat the baseline/gates; this manual implementation stage does not publish or advance the task.

## Baseline inventory

Locations refer to the baseline commit; repeated diagnostics across binary/library and test targets are listed once. All workspace targets completed successfully (exit 0, warnings enabled).

| Crate | Lint | Baseline file and line | Resolution |
|---|---|---|---|
| kanna-visual-companion | `suspicious_open_options` | `crates/visual-companion/src/tests.rs:1944` | Set `truncate(false)` explicitly; the lock fixture preserves existing bytes. |
| kanna-desktop | `dead_code` | `apps/desktop/src-tauri/src/commands/mobile/mod.rs:570` | Delete the unused retry helper and its obsolete helper-only test. |
| kanna-desktop | `too_many_arguments` | `apps/desktop/src-tauri/src/commands/companion.rs:64` | Item allow: individual Tauri arguments are the existing frontend invoke contract. |
| kanna-desktop | `too_many_arguments` | `apps/desktop/src-tauri/src/commands/companion.rs:166` | Item allow: individual Tauri arguments are the existing frontend invoke contract. |
| kanna-desktop | `needless_question_mark` | `apps/desktop/src-tauri/src/companion_bridge.rs:757` | Return the existing `Result` directly. |
| kanna-desktop | `filter_map_bool_then` | `apps/desktop/src-tauri/src/companion_bridge.rs:962` | Use `filter` followed by `map`; preserve selection and key cloning. |
| kanna-desktop | `filter_map_bool_then` | `apps/desktop/src-tauri/src/companion_bridge.rs:1076` | Use `filter` followed by `map`; preserve selection and key cloning. |
| kanna-desktop | `filter_map_bool_then` | `apps/desktop/src-tauri/src/companion_bridge.rs:1097` | Use `filter` followed by `map`; preserve selection and key cloning. |
| kanna-desktop | `type_complexity` | `apps/desktop/src-tauri/src/companion_bridge.rs:133` | Introduce the `PhasedTestBarriers` type alias; retain the underlying representation. |
| kanna-desktop | `type_complexity` | `apps/desktop/src-tauri/src/companion_bridge.rs:141` | Introduce the `PhasedTestBarriers` type alias; retain the underlying representation. |
| kanna-server | `empty_line_after_doc_comments` | `crates/kanna-server/src/transfer_sidecar.rs:276` | Move the environment documentation onto `build_transfer_sidecar_env`. |
| kanna-daemon | `question_mark` | `crates/daemon/src/agent_runtime/readers.rs:425` | Use `?` for the absent child record; retain the lock scope. |
| kanna-daemon | `too_many_arguments` | `crates/daemon/src/fanout.rs:519` | Name the writer task ownership inputs in `WriterTask`. |
| kanna-task-transfer | `too_many_arguments` | `crates/task-transfer/src/runtime/companion.rs:280` | Item allow: explicit authenticated frame bindings mirror serialized payload fields. |
| kanna-task-transfer | `too_many_arguments` | `crates/task-transfer/src/runtime/companion.rs:317` | Item allow: expected bindings remain independent from the untrusted payload under validation. |
| kanna-task-transfer | `too_many_arguments` | `crates/task-transfer/src/runtime/companion.rs:344` | Item allow: explicit authenticated control bindings mirror serialized payload fields. |
| kanna-task-transfer | `type_complexity` | `crates/task-transfer/src/runtime/companion.rs:379` | Introduce the `OwnerControlPayloadFields` type alias; retain the underlying representation. |
| kanna-task-transfer | `too_many_arguments` | `crates/task-transfer/src/runtime/companion.rs:436` | Item allow: explicit event identity and observation bindings mirror proof fields. |
| kanna-task-transfer | `too_many_arguments` | `crates/task-transfer/src/runtime/companion.rs:730` | Package opening inputs in `PeerCompanionOpen`. |
| kanna-task-transfer | `needless_late_init` | `crates/task-transfer/src/runtime/companion.rs:764` | Initialize the challenge from the existing match expression. |
| kanna-task-transfer | `too_many_arguments` | `crates/task-transfer/src/runtime/companion.rs:800` | Package observer state and admission resources in `PeerCompanionStream`. |
| kanna-task-transfer | `too_many_arguments` | `crates/task-transfer/src/runtime/companion.rs:990` | Package stream identifiers in `OwnerCompanionStream`. |
| kanna-task-transfer | `type_complexity` | `crates/task-transfer/src/runtime/lifecycle.rs:162` | Introduce the `CompanionObserverGenerations` type alias; retain the underlying representation. |
| kanna-task-transfer | `type_complexity` | `crates/task-transfer/src/runtime/lifecycle.rs:172` | Introduce the `CompanionObserverGenerations` type alias; retain the underlying representation. |
| kanna-task-transfer | `nonminimal_bool` | `crates/task-transfer/src/runtime/lifecycle.rs:708` | Use the equivalent `is_none_or` predicate. |
| kanna-task-transfer | `op_ref` | `crates/task-transfer/src/runtime/lifecycle.rs:1092` | Remove the redundant reference in the equality comparison. |
| kanna-task-transfer | `too_many_arguments` | `crates/task-transfer/src/runtime/listener.rs:2067` | Pass the existing `ListenerContext`, which already owns these dependencies. |
| kanna-task-transfer | `too_many_arguments` | `crates/task-transfer/src/runtime/peer.rs:703` | Name authenticated artifact metadata in `ArtifactStreamRequest`. |
| kanna-task-transfer | `result_large_err` | `crates/task-transfer/src/runtime/state.rs:317` | Item allow on `try_send`: preserve Tokio channel-compatible errors returning the rejected event. |
| kanna-task-transfer | `type_complexity` | `crates/task-transfer/src/runtime/state.rs:633` | Introduce the `CompanionObserverGenerations` type alias; retain the underlying representation. |
| kanna-task-transfer | `nonminimal_bool` | `crates/task-transfer/src/main.rs:301` | Use the equivalent `is_none_or` predicate. |
| kanna-task-transfer | `type_complexity` | `crates/task-transfer/src/main.rs:419` | Introduce the `CompanionIpcEventClass` type alias; retain the underlying representation. |
| kanna-server | `dead_code` | `crates/kanna-server/src/http_api.rs:58` | Gate the unauthenticated in-process route-test helper with `cfg(test)`. |
| kanna-server | `dead_code` | `crates/kanna-server/src/http_api/router.rs:441` | Gate the unauthenticated in-process route-test helper with `cfg(test)`. |
| kanna-server | `dead_code` | `crates/kanna-server/src/relay.rs:1511` | Delete the unreferenced `relay_error_event` function. |
| kanna-server | `dead_code` | `crates/kanna-server/src/task_creator/lifecycle.rs:37` | Gate the test-only spawn primitive wrapper with `cfg(test)`. |
| kanna-server | `dead_code` | `crates/kanna-server/src/task_creator/lifecycle.rs:66` | Gate the error conversion used by the test-only wrapper with `cfg(test)`. |
| kanna-server | `dead_code` | `crates/kanna-server/src/task_creator/lifecycle.rs:221` | Delete the unreferenced API spawn wrapper. |
| kanna-server | `dead_code` | `crates/kanna-server/src/task_creator/stages.rs:1119` | Delete the unused budget predicate and its helper-only test; route revision-budget coverage remains. |
| kanna-server | `dead_code` | `crates/kanna-server/src/task_creator/types.rs:115` | Delete the unused `PreparedTaskSpawn` accessor; retain the used stage-run accessor. |
| kanna-server | `too_many_arguments` | `crates/kanna-server/src/ksp.rs:3416` | Name the retention count and wakeup pair in `CompanionScanRetention`. |
| kanna-server | `collapsible_if` | `crates/kanna-server/src/task_creator/definition_source.rs:100` | Combine the nested checks with short-circuit `&&`. |
| kanna-server | `too_many_arguments` | `crates/kanna-server/src/task_creator/mod.rs:1408` | Item allow: keep explicit workspace inputs aligned with the delegated with-extra variant. |
| kanna-server | `too_many_arguments` | `crates/kanna-server/src/task_creator/mod.rs:3084` | Pass the provider and session type stamp as a pair. |
| kanna-server | `explicit_counter_loop` | `crates/kanna-server/src/task_files.rs:258` | Use `enumerate`; preserve the exact visit limit and incomplete flag. |
| kanna-server | `while_let_loop` | `crates/kanna-server/src/transfer_sidecar.rs:831` | Read lengths with `while let Ok`; retain framing and termination behavior. |
| kanna-server | `await_holding_lock` | `crates/kanna-server/src/http_api/tests/actions.rs:1872` | Await the shared Tokio mutex; synchronous fixtures use its blocking acquisition outside runtimes. |
| kanna-server | `await_holding_lock` | `crates/kanna-server/src/http_api/tests/actions.rs:2327` | Await the shared Tokio mutex; synchronous fixtures use its blocking acquisition outside runtimes. |
| kanna-server | `await_holding_lock` | `crates/kanna-server/src/http_api/tests/actions.rs:3137` | Await the shared Tokio mutex; synchronous fixtures use its blocking acquisition outside runtimes. |
| kanna-server | `await_holding_lock` | `crates/kanna-server/src/http_api/tests/actions.rs:3383` | Await the shared Tokio mutex; synchronous fixtures use its blocking acquisition outside runtimes. |
| kanna-server | `await_holding_lock` | `crates/kanna-server/src/http_api/tests/actions.rs:3662` | Await the shared Tokio mutex; synchronous fixtures use its blocking acquisition outside runtimes. |
| kanna-server | `await_holding_lock` | `crates/kanna-server/src/http_api/tests/actions.rs:4557` | Await the shared Tokio mutex; synchronous fixtures use its blocking acquisition outside runtimes. |
| kanna-server | `await_holding_lock` | `crates/kanna-server/src/http_api/tests/actions.rs:5001` | Await the shared Tokio mutex; synchronous fixtures use its blocking acquisition outside runtimes. |
| kanna-server | `await_holding_lock` | `crates/kanna-server/src/http_api/tests/actions.rs:6144` | Await the shared Tokio mutex; synchronous fixtures use its blocking acquisition outside runtimes. |
| kanna-server | `await_holding_lock` | `crates/kanna-server/src/http_api/tests/actions.rs:6448` | Await the shared Tokio mutex; synchronous fixtures use its blocking acquisition outside runtimes. |
| kanna-server | `await_holding_lock` | `crates/kanna-server/src/http_api/tests/actions.rs:6650` | Await the shared Tokio mutex; synchronous fixtures use its blocking acquisition outside runtimes. |
| kanna-server | `await_holding_lock` | `crates/kanna-server/src/http_api/tests/actions.rs:6871` | Await the shared Tokio mutex; synchronous fixtures use its blocking acquisition outside runtimes. |
| kanna-server | `await_holding_lock` | `crates/kanna-server/src/http_api/tests/create_task.rs:1676` | Await the shared Tokio mutex; synchronous fixtures use its blocking acquisition outside runtimes. |
| kanna-server | `type_complexity` | `crates/kanna-server/src/http_api/tests/input.rs:3040` | Introduce the `RecordedInputs` type alias; retain the underlying representation. |
| kanna-server | `type_complexity` | `crates/kanna-server/src/http_api/tests/input.rs:3203` | Introduce the `RecordedInputs` type alias; retain the underlying representation. |
| kanna-server | `type_complexity` | `crates/kanna-server/src/http_api/tests/input.rs:3204` | Introduce the `RecordedInputs` type alias; retain the underlying representation. |
| kanna-server | `bool_assert_comparison` | `crates/kanna-server/src/http_api/tests/repo_definitions.rs:698` | Use `assert!` with the same negated predicate. |
| kanna-server | `needless_option_as_deref` | `crates/kanna-server/src/http_api/tests/revision_status.rs:557` | Remove the redundant conversion of `Option<&str>`. |
| kanna-server | `needless_option_as_deref` | `crates/kanna-server/src/http_api/tests/revision_status.rs:597` | Remove the redundant conversion of `Option<&str>`. |
| kanna-server | `nonminimal_bool` | `crates/kanna-server/src/ksp.rs:9221` | Use the equivalent `is_none_or` predicate. |
| kanna-server | `items_after_test_module` | `crates/kanna-server/src/task_creator/worktree.rs:201` | Move the test module after production items, preserving documentation. |
| kanna-server | `type_complexity` | `crates/kanna-server/src/task_creator/tests/core.rs:234` | Introduce the `ReopenState` type alias; retain the underlying representation. |
| kanna-server | `type_complexity` | `crates/kanna-server/src/task_creator/tests/core.rs:264` | Introduce the `ReopenState` type alias; retain the underlying representation. |
| kanna-server | `err_expect` | `crates/kanna-server/src/task_creator/tests/core.rs:2853` | Use `expect_err` with the same failure message. |
| kanna-server | `err_expect` | `crates/kanna-server/src/task_creator/tests/core.rs:2875` | Use `expect_err` with the same failure message. |
| kanna-server | `err_expect` | `crates/kanna-server/src/task_creator/tests/core.rs:2918` | Use `expect_err` with the same failure message. |
| kanna-server | `err_expect` | `crates/kanna-server/src/task_creator/tests/core.rs:2961` | Use `expect_err` with the same failure message. |
| kanna-server | `await_holding_lock` | `crates/kanna-server/src/task_creator/tests/setup.rs:186` | Await the shared Tokio mutex; synchronous fixtures use its blocking acquisition outside runtimes. |
| kanna-server | `await_holding_lock` | `crates/kanna-server/src/task_creator/tests/setup.rs:423` | Await the shared Tokio mutex; synchronous fixtures use its blocking acquisition outside runtimes. |
| kanna-server | `await_holding_lock` | `crates/kanna-server/src/task_creator/tests/setup.rs:503` | Await the shared Tokio mutex; synchronous fixtures use its blocking acquisition outside runtimes. |
| kanna-server | `await_holding_lock` | `crates/kanna-server/src/task_creator/tests/setup.rs:689` | Await the shared Tokio mutex; synchronous fixtures use its blocking acquisition outside runtimes. |
| kanna-server | `items_after_test_module` | `crates/kanna-server/src/transfer_control.rs:622` | Move the test module after production items, preserving documentation. |

The desktop build script also reported missing staged sidecars (`apps/desktop/src-tauri/build.rs`, cargo build warning rather than a Rust lint). The canonical lane stages these prerequisites before clippy; no diagnostic suppression or build behavior change is needed.

## Validation and review handoff

Baseline: 77 distinct Rust warning locations (kanna-visual-companion: 1, kanna-desktop: 9, kanna-server: 46, kanna-daemon: 2, kanna-task-transfer: 19). Other workspace crates emitted no Rust warnings.

- Strict workspace/all-target clippy passed.
- kd Rust orchestration: 4 tests passed, including failing before test binaries on clippy failure.
- `pnpm exec tsc --noEmit` passed from `tools/kd`; the repository root has no tsconfig and prints compiler help.
- `cargo fmt --all` and its check mode passed. Full `./kd test all` passed on the fourth attempt, with default test settings, including strict clippy before the workspace tests and the serialized daemon lane.
- Existing sidecar/route and TCP transfer tests retain their assertions and exercise the updated async-lock/call wiring. No rendered UI changed.
- No dependencies changed; neither Cargo lockfile needs regeneration.
- Eight new allows are item-scoped and commented (two Tauri commands, four cryptographic binding helpers, one teardown delegator, one Tokio-compatible channel wrapper). No await-lock allows were added; the obsolete sidecar-test allow was removed.

In-flight coordination check on 2026-09-05 against the connected staging task API: 756c4972 is at `pr`, 0108b94d at `review`; neither is closed. Do not assume either has merged at the next stage. The pre-PR rebase and repeated warning inventory remain required.

First full gate attempt: JS/TS (17 tasks), desktop Rust, and strict clippy passed. Server tests had 1,239 passes and one failure: `review_run_binding_refuses_cross_task_pair_and_allows_concurrent_verdicts` received HTTP 500 instead of 200 for its second concurrent request. The unchanged failing test passed immediately in isolation (0.76 s). No runtime or test behavior was altered to bypass it; it passed in subsequent full runs.

Second full gate attempt: all 1,240 server unit tests and server integration tests passed. Task-transfer runtime integration had 136 passes and one mDNS pairing timeout in `concurrent_mdns_test_runs_keep_their_peers_apart` (15 s). This unchanged discovery/pairing test passed in isolation (1.00 s). All 137 tests in its full integration binary then passed (11.04 s). Discovery and pairing behavior remain outside warning-cleanup scope.

Third full gate attempt stopped in the JS/TS lane: kd `process-inventory.test.ts` / `serializes concurrent writers without losing resources` lost fixture `socket-2` (733 other kd tests passed). That unchanged inventory test passed in both previous full runs. No inventory behavior was changed; it passed in the fourth full gate.

Final full gate on 2026-09-05: `Canonical Rust tests passed.` and `Canonical local verification passed.` (exit 0). All touched crates passed their tests. The three intermittent failures above are separate follow-up issues, not changes included in this task.
