# Task b6048ec2: Rewrite the README

## Goal

Rewrite `README.md` to accurately present Kanna's current multi-agent,
multi-machine workflow system, mobile experience, supported CLI providers, and
installation path using the owner's 2026-09-03 positioning brief as the tone
and claims baseline.

## Scope and constraints

- Verify user-facing claims against the repository, including providers,
  workflows, install URL, platform support, task management, mobile LAN/relay
  behavior, and task transfer.
- Preserve the existing developer, build, release, schema-publishing, and MIT
  license information below a concise user-facing introduction, trimming only
  duplication with `docs/dev/` and not changing its technical meaning.
- Embed three to five optimized owner screenshots from `~/kanna-pics` or
  `~/Downloads/kanna-pics` when readable; otherwise add explicit HTML TODO
  placeholders for desktop, workflow/review, mobile terminal, and notification
  shots.
- Keep the change Markdown-only. Do not add unsupported claims, API-key
  handling claims beyond what the code supports, invented pricing details, or
  rendered-UI changes.

## Directive update

- The manager supplied four optimized WebP screenshots on 2026-09-03 and
  directed that they be inspected, renamed descriptively, and embedded with
  alt text in place of the fallback TODO placeholders. The manager also said
  the full test suite did not need to be rerun for the image follow-up.

## Done when

The README matches the verified current product, names exactly `claude`,
`codex`, `copilot`, `opencode`, and `antigravity`, uses the
`tampopogk/kanna` install URL, states the owner's mobile pricing wording,
contains screenshots or explicit placeholders, links configuration and deeper
documentation, preserves the MIT license, and `./kd test all` passes (with any
docs-lint outcome recorded).

## Verification record

- All four supplied screenshots were inspected, renamed for their contents,
  and embedded in the README with descriptive alt text.
- The repository has no Markdownlint configuration or dependency, so no
  Markdownlint command was run.
- `./kd test all` passed. An initial cold run hit two timing-sensitive server
  test timeouts; both passed unchanged in isolation, and the complete warm
  rerun passed. Per the manager's later directive, it was not rerun after the
  screenshot-only follow-up.
