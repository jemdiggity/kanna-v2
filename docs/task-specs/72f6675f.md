# Task 72f6675f — Architect: IAP-from-start addendum to the billing spec

Goal: architect consultation amending `docs/specs/accounts-and-billing.md`
(merged via PR #1137, task `da6b6800`) per the owner's 2026-08-20 decisions:
(1) "Let's include iap from the start" — Apple In-App Purchase becomes a
launch channel alongside web Stripe, not a review-forced fallback; and
(2, delivered mid-task) "Also I'll need a leech flag for users who don't need
to pay" — an admin-granted complimentary (`comp`) entitlement source.

Scope: amend the spec document only (interleaved edits + a new Apple IAP
decision section, later decisions renumbered), covering the StoreKit-2 shape,
ASSN v2 authority, appAccountToken binding, restore/relink policy,
source reconciliation (three writers + one reducer, comp precedence),
in-app registration/deletion, the 3.1.2 compliance surface, pricing parity,
sequencing/App Store timeline, and required test coverage. No product code,
tests, config, or follow-up tasks.

Done means: the spec answers all seven prompt design points plus the comp
addition with verified evidence, keeps owner decisions named with defaults,
and one verdict summarizing the deltas is recorded via `kanna_complete_stage`.
