# Mobile composer native reset

## Goal

After Send clears a multiline task-detail reply, remount/reset the rendered native composer so it returns from its expanded height to the 40 pt one-line height on iOS 26.5/Xcode 26.6.

## Scope and constraints

Preserve `verifyRelayComposerResetJourney`'s native height, cleared value, and keyboard-dismissal assertions; add regression coverage and verify the named iPhone 17 Pro simulator. No relay-contract changes or weaker E2E expectation.

## Done when

A three-line reply expands the native composer, then Send clears it, dismisses the keyboard, and renders the input at one line. Focused product and relay-journey harness tests pass; task 182f8d59 runs the full relay journey from its branch, which contains the required relay setup fixes.

## Coordination

On 2026-09-05, task 182f8d59 confirmed the exact iPhone 17 Pro / iOS 26.5 repro and asked that `verifyRelayComposerResetJourney` remain unchanged. Its branch contains the relay setup fixes absent from this branch, so it will run the authoritative full relay lane after receiving product commit `ab251b366`; this task retains focused product coverage and named-simulator build/install verification. The booted simulator was launched through `./kd mobile run --simulator C48044E2-D11B-4A50-993D-D571CA8462E7`; without the relay fixture it rendered only the dev-client blank screen, so this task does not claim a completed visual journey.

Task 182f8d59 then completed the authoritative cold iOS 26.5 iPhone 17 Pro run with product commit `865e8d2ddf9973f0188f94e5d0621aa0846014e1` integrated: `KANNA_IOS_SIMULATOR_NAME='iPhone 17 Pro' ./kd test remote-e2e --dev --mobile-relay` passed end to end, including the unchanged strict `verifyRelayComposerResetJourney` assertion (cleared value, dismissed keyboard, and 40 pt native height). Its durable run record is `docs/task-specs/182f8d59.md` at commit `882da2363` on branch `task-182f8d59`.
