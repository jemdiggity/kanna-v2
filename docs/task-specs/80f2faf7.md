# Task 80f2faf7 — refresh docs/dev/ to match main

Owner request 2026-08-21: "update the developer documentation — I need to know
how kanna works!" Audience: owner/operator first, contributors second. The
docs/dev/ pages must let a smart reader build an accurate mental model of the
system as it exists on main today.

## Goal

Bring `docs/dev/*.md` (architecture, dev-workflow, getting-started, testing,
release, product-behavior, README) up to date with current code. Every claim
kept or written is verified against code; stale claims are fixed or deleted.

## Scope

- In: docs/dev/*.md content updates; linking (not duplicating) specs in
  docs/specs/; a short "what changed in this refresh" note in the PR
  description (a later stage writes the PR — the note is recorded at the end
  of this spec for that stage to use).
- Out: rewriting AGENTS.md (stale facts there are listed in the run summary
  for the manager instead); rewriting docs/specs/ or
  docs/kanna-server-boundary.md; any code changes.
- File structure stays as-is unless a split/merge clearly serves the reader.

## Constraints

- Accuracy over coverage: no claim without having read the code (or a spec
  that provably matches the code).
- Recent-change checklist from the task prompt: workflow engine (fork-point
  invariant #1154, revision resume + empty-feedback guard #1142, revision
  budget 5, task specs, posts, completion vocabulary), process lifecycle
  (.kanna/kd-state inventory, TERM→KILL, task.teardown_failed, background-
  process contract #1141), input pipeline (task_input ledger, ok-means-
  submitted, composer attestation #1123/#1130/#1134/#1147, awaiting_input vs
  activity_changed), relay & cloud (VM+Caddy deploy, tunnel classes, byte
  odometer, /stats + dashboard + kd relay stats, perMessageDeflate, credential
  cache, maxPayload/pre-auth caps, diff-aware Firestore publication, ksp
  task-summary streaming, entitlement flag + comp seeding, scaling plan link,
  preview spec link), billing (functions backend, web portal + hosting target,
  kd cloud deploy targets, .firebaserc mapping guard #1161), release flow
  (current RC semantics), mobile (pins/dismiss, commit-on-release swipe,
  stage-colored cards, snippet overlay, QR pairing, persistent control socket
  #1155, photo attachment), architecture data-flow refresh (event feed, ks1
  aggregated cross-machine cursor).

## Findings that shape the content

- Task 8275f44c (semver-regression refusal + automatic series continuation in
  `kd release ship --staging`) merged as PR #1162 (origin/main 36a11b931)
  during this task — release.md documents the merged behavior: the derived
  staging version must be strictly greater than the active channel version by
  semver ordering, and a bare same-source-branch ship continues the unpromoted
  active `X.Y.Z-staging.N` series rather than re-deriving from `VERSION`;
  explicit bump flags select a new derivation and still cannot roll back.
- Task 6b9fb72a merged as PR #1164 (origin/main 53f4dbfef) during review —
  and an owner correction *replaced* the original `task.idle_sustained`
  design: `task.activity_changed` is now the provider-neutral, server-
  debounced settled transition (default 20 s, every direction, payload
  carries `runtimeState` + `latestRunFinishedWithoutCompletion`); no
  `task.idle_sustained` event exists. The docs describe the merged contract.
- PR #1163 (origin/main dbac0f999) merged during review and changed the
  billing catalog: `STRIPE_PRICE_MONTHLY`/`STRIPE_PRICE_ANNUAL` are no longer
  function secrets, checkout is monthly-only and resolves the caller's
  currency by `cloud_monthly_<currency>` lookup key at session time, the
  catalog is provisioned in Stripe by
  `pnpm --filter @kanna/firebase-functions stripe:provision`, and the Stripe
  accounts now exist. release.md reflects this.
- The relay runs on a single VM per environment behind Caddy via docker
  compose (see docs/relay-vm-operations.md), not Cloud Run; architecture.md's
  Cloud Run claim was stale and is corrected.

## Done when

- docs/dev/*.md read true against main; contradictions that could not be
  resolved by reading are recorded below as open questions.
- Doc-only verification passes: `pnpm test` (and any doc lint the repo has).

## PR-description note ("what changed in this refresh") — for the pr stage

> Docs-only refresh of docs/dev/ against main (2026-08-21), every claim
> verified against code. Architecture: relay corrected to Node 22 on a GCE
> VM behind Caddy (was "Bun on Cloud Run"); added the account portal, billing
> functions/entitlement reducer, tunnel classes, byte odometer + /stats +
> dashboard + `kd relay stats`, compression/caches/abuse bounds, entitlement
> flag, and a new section mapping the task event feed (ks1/km1), the durable
> input pipeline + composer attestation, the completion-notify vocabulary,
> and diff-aware cloud task publication with KSP task-summary streaming.
> Dev-workflow: process-inventory contract expanded (Rust daemon writer,
> spawn identity, TERM→KILL revalidation, lock protocol, agent
> background-process rule); cloud/release command block completed. Release:
> staging version derivation with automatic series continuation and the
> strict forward-version gate (PR #1162), retention pruning, ship
> preconditions, post-promotion trunk resumption, release-policy validation
> reality.
> Product-behavior: fixed shortcuts/activity/sidebar-ordering staleness,
> added runtimeState/readState, close refusal on open subtasks, the
> revision-feedback guard, and a new Mobile app section. Code/doc
> contradictions found but out of scope are listed as open questions in
> docs/task-specs/80f2faf7.md. Revision 1 (post PR #1162 merge): release.md
> now documents the staging forward-version gate and automatic series
> continuation as merged behavior, and the architecture event-feed and
> relay-cache claims were tightened to match the code. Revision 2 (post PR
> #1163/#1164 merges): billing docs now describe the Stripe price catalog
> (lookup-key resolution, `stripe:provision`, price-id secrets gone, account
> exists); the event-feed section documents the merged server-debounced
> `task.activity_changed` contract (no `task.idle_sustained`); the task
> creation/runtime ownership path, the revision-resume/budget/task-spec
> contracts, the maxPayload derivation + override, and the complete
> resting-snippet boundaries were added or corrected.

## Revision 1 (2026-08-21, reviewer feedback)

Three docs-only corrections, all applied: (1) task 8275f44c merged as PR
#1162 while this task was in review — release.md and this spec now describe
the merged staging forward-version gate and automatic series continuation
instead of "ancestry-only, fix in flight"; (2) architecture.md's event-feed
claim corrected to the actual invariant (events are appended by the DB calls
that emit the state changes they describe, in the same transaction where one
exists — not "every DB write", and no false `task.created`→`task.teardown_failed`
range); (3) architecture.md's relay-cache sentence qualified — only new
desktop-credential connections always re-read Firestore; entitlement
resolution at authentication may hit the 60 s entitlement cache.

## Revision 2 (2026-08-21, reviewer feedback)

Five docs-only corrections, all applied: (1) release.md's billing section
reconciled with PR #1163 (price-id secrets removed, lookup-key checkout,
`stripe:provision` script, Stripe account exists); (2) architecture.md's task
creation/runtime path now shows the real ownership chain — frontend store →
`kanna-server` (`createDesktopTask`) → DB/worktree/setup → daemon
`SpawnAgent`, with the terminal watcher persisting runtime/activity/read
effects — and product-behavior's create steps were aligned; (3) the
revision-resume, budget (5, `0` unlimited), exhausted-parking-vs-human-reset,
and committed-task-spec contracts are now explained in product-behavior.md
with a pointer from architecture.md; (4) the event-feed section documents the
awaiting_input vs activity_changed distinction per the merged PR #1164
server-debounce contract (`task.idle_sustained` was never added — the owner
correction replaced that design); (5) maxPayload's derivation and
`KANNA_RELAY_MAX_PAYLOAD_BYTES` override are stated, and the resting-snippet
boundaries now include `working`→`unread` and `transition_revision` changes.

## Stale AGENTS.md facts (for the manager — AGENTS.md deliberately not edited)

- AGENTS.md's revision bullet ("Revisions resume by default…") still says
  `request_revision` reopens the previous **Claude** session and lists
  "non-Claude provider" as a fresh-fork precondition. Stale:
  `crates/kanna-server/src/task_creator/resume.rs` resumes Claude, Copilot,
  Codex, and OpenCode when their recorded session/transcript preconditions
  hold; only Antigravity and headless SDK sessions can never resume.
- `.kanna/workflows/schema.json` still describes `revision_limit` as
  "Defaults to 3 when omitted"; the code default is 5 (also open question 2
  below — a live code-asset bug, not an AGENTS.md sentence, listed here so
  the manager sees both).

## Open questions / contradictions found (recorded, not fixed — outside scope)

1. **Portal ↔ billing-backend checkout contract mismatch** (verified in code,
   still present on origin/main after PR #1163, unresolvable by docs): the
   portal calls `createCheckoutSession` with `{successUrl, cancelUrl}` and no
   `plan` or `currency` (`apps/web-portal/src/types.ts`), but the backend
   requires `plan: "monthly"` (post-#1163; `"annual"` was removed), ignores
   the client URLs, and builds return paths `/billing/success` /
   `/billing/canceled` — routes the portal does not register (it has
   `/checkout/success` and `/checkout/cancelled`). Each side's tests pin its
   own spelling, so nothing fails today; a live call would. Also, the backend
   may return `url: null`, which the portal types as `string`.
2. `.kanna/workflows/schema.json` still describes `revision_limit` as
   "defaults to 3"; the code default is 5
   (`crates/kanna-server/src/task_creator/definitions.rs:210`). Live
   code-asset bug, not a docs/dev file.
3. `docs/relay-vm-operations.md` glosses `/stats`' `connections` as "paired
   desktop/phone connections"; it is the number of userIds with an active
   connection pair (`services/relay/src/router.ts:844-846`).
4. `apps/web-portal/README.md:13` still says "Until the billing backend
   lands…" — it has landed; the test stub remains but the framing is stale.
5. `docs/kanna-server-boundary.md` CLI section (~line 1125) still describes
   pre-#1134 send-input semantics (retained-behind-draft prints ok) and omits
   `kanna-cli task inputs` and `--source`; its own earlier "Terminal Input
   Boundaries" section states the corrected contract.
6. Dead relay-era leftovers: `services/relay/deploy/compose.yaml` (nothing
   consumes it) and the `KANNA_CLOUD_RUN_REGION` variable name in
   `services/relay/deploy/provision.sh:7`.
