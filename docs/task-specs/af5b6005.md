# Task af5b6005: inspect process-termination guidance

Goal: Answer whether `pkill`/`killall` guidance was added to every built-in agent definition, and identify its origin and rationale.

Scope: Remove the duplicated process-termination paragraph from all 21 built-in agent definitions and remove empty headings left behind. Keep the canonical guidance in repository `AGENTS.md`; update the existing asset test to reflect that ownership. No runtime changes.

Done: Guidance exists only in repository conventions, agent asset tests pass, and the affected TypeScript package typechecks.

Owner directive (2026-09-04): After the initial investigation, the owner said “that belongs in the agents.md”, replacing the investigation-only scope with the consolidation above.
