# Task 86f10a2c: truthful task git stats

## Goal

Make task-detail git statistics use the same remote-first base-ref resolution as task diffs and distinguish an unresolved base from a genuine zero-ahead/zero-behind result.

## Scope

- Share one resolver that prefers `origin/<base>` and then local `<base>` for both task diffs and task stats.
- If a persisted task base no longer resolves, retry the repo's current default branch for stats; do not modify repo registration or reconciliation owned by the companion task.
- Keep the task-detail payload backward-compatible for mobile by omitting unavailable commit counts and adding an explicit unresolved marker.
- Add HTTP-boundary coverage for remote-only and wholly missing base refs.
- Cover the confirmed incident shape where local `<base>` exists but is stale
  behind `origin/<base>`; the remote ref must win.

## Owner directives

- 2026-09-03: the owner relayed the affected-machine finding that stale local
  `main` masked the true `origin/main...HEAD` result, confirmed remote-first
  resolution as the operative fix, required stale-local coverage, and asked
  this task to coordinate a reusable resolver with companion task `47b7cb85`.
- 2026-09-03: companion-task coordination clarified that task creation needs a
  resolved ref even when it is unrelated to checkout `HEAD`; the shared result
  therefore exposes `reference` independently from an optional `merge_base`.
- 2026-09-03: companion task `47b7cb85` found that valid local branch names
  beginning `--` require Git option terminators; the shared resolver now passes
  `--end-of-options` to `rev-parse` and `--` to `merge-base`.

## Done

The required cross-boundary tests pass, Rust is formatted, mobile TypeScript
type-checks, and clippy introduces no warnings in the changed code.
