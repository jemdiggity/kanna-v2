# Mobile task identity visibility

## Goal

Keep a durable task ID visible with several-hundred-character prompts in mobile list rows and collapsed or expanded task-detail headers.

## Scope and constraints

- Change only `apps/mobile` and documentation. Keep the expanded detail identity outside the prompt scroller; show the collapsed ID whenever a durable/displayable ID exists; place list IDs beside titles while middle-ellipsizing long IDs so titles remain legible.
- Cover these layouts with unit and LAN/Appium smoke E2E tests, and visually verify them in a separate iOS 26.5 simulator. This is JS-only: runtimeVersion remains `2.2.3`.

## Done when

Long prompts leave the ID visible in both detail-header states, 64-hex IDs and long titles are both legible in rows, product behavior matches, checks pass, and simulator screenshots plus build/runtime details are recorded here.

## Verification record

- 2026-09-05: `./kd mobile run --simulator "iPhone 17 Pro Max"` built and installed the dev client on iOS 26.5 (UDID `D7905476-A91C-438C-97A3-E0210FD12B0B`), deliberately separate from the booted iPhone 17 Pro. The tested native identity was `build.kanna.app.dev`; its configured runtimeVersion is `2.2.3`.
- The client did not load the worktree Metro bundle, so visual task-state screenshots could not be captured: the Expo development launcher reported `Failed to load app from http://127.0.0.1:8189` / `Failed to open app`, although `http://127.0.0.1:8189/status` returned `packager-status:running`. No render pass is claimed; focused tests, the full mobile Vitest suite, and TypeScript checks cover the layout until the launcher issue is resolved.
- Follow-up diagnosis, 2026-09-05: 8189 was not this worktree's Metro. `lsof` identified PID 13702 from `/Volumes/VHS/temp/mobile-reinstall/apps/mobile` (staging); this worktree's `kd` session was down and its `mobile` pane was blocked at Expo's "Use port 8190 instead?" prompt. Restarting this worktree with `KANNA_MOBILE_PORT=8191 ./kd dev up --mobile` produced PID 47489, listening on 8191, and Metro bundled this worktree's `apps/mobile/index.js` at `exp+kanna-mobile://expo-development-client/?url=http://172.31.32.99:8191`. The client reaches the app, but is unpaired with the worktree desktop, so creating and opening the required long-prompt task remains pending pairing/UI setup.
