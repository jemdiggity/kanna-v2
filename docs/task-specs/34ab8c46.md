# Task 34ab8c46

## Goal

Advancing a task from `in progress` must deliver the stage's `commit` post prompt as one complete submitted message, without leaving or displaying a partial paste in the agent composer.

The reported production example is Ami task `afe8e9d8`: its post run was recorded as running, while the Claude composer received and submitted only `sult:`, the tail of the prompt's `Previous implementation result:` text. The owner pointed to this task on 2026-09-02 as the concrete reproduction.

## Scope

- Trace and correct the stage-advance/post delivery path across the server and PTY daemon boundary.
- Add regression coverage at the boundary where the truncation or partial submission occurs.
- Do not change raw/user terminal paste behavior or workflow transition semantics beyond what is required for complete post delivery.
- Operational recovery: on 2026-09-02 the owner directed, “task-8ba4013e and task-afe8e9d8 are stuck in the commit post-stage. unstick them.” Record each clean commit post as successful and advance both tasks without changing their Ami worktrees.

## Done when

The complete commit-post prompt reaches the live agent session as one multiline paste with its separate submission boundary intact, single-line input behavior is unchanged, boundary-level regression coverage passes, relevant Rust formatting/lint checks are clean, and both identified Ami tasks have left the commit post for their `pr` stage.
