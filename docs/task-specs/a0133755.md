# Task a0133755: provider-resume review follow-up

## Goal

Address the two non-blocking findings from PR #1269: make desktop task-session recovery wait for the daemon's `session_created` event instead of polling `list_sessions`, and make the server's finalized dead-run summary say that automatic recovery was started by the resume action.

## Scope and constraints

- Keep the resume path otherwise unchanged: the desktop must call the server action, never spawn locally, retain the 30-second timeout and existing error text, and refresh task state when the replacement session is created.
- Register the event waiter before calling the resume action. The server transition is detached, so registering after the HTTP response would race a fast `session_created` event; no fallback poll is permitted.
- Update the existing 2026-09-03 E2E-gap note if current coverage still cannot exercise the full daemon-death path. There is no rendered-UI change and no screenshot requirement.

## Done when

Recovery completes from `session_created` with no recovery-path `list_sessions` call, the dead-run summary accurately records that automatic provider-context recovery began, the focused desktop/server tests cover the behavior, and `./kd test all` passes.
