# Task 231ad8fc — Rethink the in-app PR review experience

## Goal

Produce a **design assessment only**: a bounded design and implementation plan
for a Kanna review experience that lets an operator meaningfully review a PR
without opening GitHub.

## Origin

Owner directive delivered into task `f1c3ca89` on 2026-09-05 (speech-to-text,
`kanna_task_inputs` id 148), verbatim in the relevant part:

> "…the problem is our PR approval feature sucks so I would suggest that we
> remove it and launch another task to rethink it. The idea is to let the user
> actually review PRs so they don't actually have to go to GitHub to review
> them. That's the idea — to devalue GitHub."

The same directive said mobile advance-stage should behave the way desktop's
does, and confirmed that moving a PR-stage task with a post action to enqueue it
for the merge master "is OK". Removal of the current UI is being clarified in
`f1c3ca89` and is **out of scope here**.

## Redirect — owner directive, 2026-09-05 (this task's session)

After the first design was delivered, the owner redirected the approach by
dictation from the mobile app. Verbatim, in the load-bearing part:

> "…what I wanna do for a PR review is basically use the building blocks we
> already have, which is structured workflows and agent definitions. Let's
> define a PR review manager agent that helps the user determine which PR they
> want to review and which order they should be reviewed in. Then that agent
> will create a bunch of child tasks of PR review agents, each one of those
> agents reviews one single PR, and their workspace worktree is based on the
> branch of the PR. The user can use the diff tool to look at what changed. It
> would be nice if the base branch were set so that the diff tool on first open
> shows the branch diff from the base branch to the tip. The PR review agent
> should also help the user identify risky areas of code change — maybe those
> parts of their system that are more critical than others — and more or less
> make the reviewing task easier for the human."

What this changed:

- **Withdrawn**: the first design's bespoke three-pane review surface, its
  approval-attestation schema (two `pipeline_item` columns plus a
  `task.review_recorded` event), the `reviewContext` repo command and PR tab,
  and the mobile review screen. The earlier open question about accepting the
  attestation columns is moot.
- **Replaces it**: a dispatched PR review task tree composed from existing
  primitives — a `pr-review` workflow and manager agent that triages and orders
  open PRs, fanning out one child task per PR on an internal
  `pr-review-single` workflow whose worktree is forked from the PR head; the
  human reviews in the existing diff tool.
- **Kept from the first design**: the diagnosis of the diff tool's gaps, now
  carried as an explicitly deferred list rather than as work this design does.
- The design document was renamed `docs/specs/pr-review-surface.md` →
  `docs/specs/pr-review-dispatch.md` and rewritten. The old title named the
  withdrawn approach.

## Second directive — defaults, extension, and setup (owner, 2026-09-05)

> "Kanna should ship with sensible default agent definitions, but allow
> extension or replacement, and the initial setup agent should help the user
> make these agent modifications for each of their repos when they're initially
> imported or created. The user may want either option: their own PRs or if
> they're senior level maybe they're responsible for the whole repo. Let's
> exercise that now but leaving it undefined in the built-in agent and
> extending it in our repo that we review all the PRs."

What this changed:

- Resolves the design's open question about PR review scope: the built-in
  `pr-triage` **leaves scope undefined and asks**; a repo answers by
  `EXTEND.md`; the `setup` agent asks the question at repo import and writes
  the file. Captured in the design as "Defaults, extension, and setup", which
  states the rule generally, not only for this one question.
- **Adds one file to this task's deliverables**, on the owner's explicit
  instruction to exercise it now:
  `.kanna/agents/pr-triage/EXTEND.md`, answering "every open PR" for
  the Kanna repository. It is committed ahead of the agent it extends and is
  inert until phase 1 — verified in `definitions.rs`: an `EXTEND.md` whose base
  agent does not resolve is skipped, and a directory holding only an
  `EXTEND.md` for a non-built-in name never enters the `agents()` listing, so
  it is neither an error nor a listing.
- **Required a test fix**, discovered by running the suite:
  `packages/core/src/workflow/qa-assets.test.ts` enumerated every directory
  under `.kanna/agents` and read an `AGENT.md` from each, so an
  extension-only directory made two tests throw `ENOENT`. The tests are about
  built-in agent *definitions*, so they now filter to directories that
  actually contain an `AGENT.md`. This was latent: any repo-local `EXTEND.md`
  for a non-built-in agent would have broken them.
- Not touched: `.kanna/agents/setup/AGENT.md`. Adding its fifth question is a
  built-in change and therefore phase-1 implementation, which this task does
  not do; the question's exact wording and its inspection-based pre-answer are
  specified in the design instead.

## Third directive — "use your best judgment" (owner, 2026-09-05)

The owner delegated the design's three remaining open questions rather than
answering them. Decisions taken, all recorded in the design's "Resolved"
section so a reviewer sees them as decisions and not as drift:

- **Forge authority.** The child may *transcribe* — post the human's own review
  words or change request to the PR, on an explicit instruction, restating them
  first — because a verdict the human must retype on GitHub defeats the
  directive. It may not *exercise authority*: approving or dismissing reviews is
  not in the built-in and belongs to a repo's own `EXTEND.md`. Merging stays the
  merge master's, extension or not.
- **Naming.** Workflows `pr-review` (public) / `pr-review-single` (internal);
  agents `pr-triage` / `pr-reviewer`. The manager is `pr-triage` rather than the
  owner's word "manager", because `-manager` in this repository means
  `task-manager`'s shape — a long-running event loop — and this agent is a
  conversational session that parks. Flagged in the design as reversible by one
  word. The already-committed extension was renamed with it.
- **The diff tool.** The file list is *in* this effort, as phase 4, scoped to
  numstat + file list + viewed-tracking and explicitly parallel to phases 2-3
  rather than behind them. The 1 MiB cap and a mobile review flow stay
  deferred.

## Fourth directive — "let's just push it forward. I'll dog food it." (owner, 2026-09-05)

The task stops being design-only. The owner asked for the design to be built so
they can use it, so **phase 1 is implemented on this branch**:

- `.kanna/workflows/pr-review.json` (public) and
  `.kanna/workflows/pr-review-single.json` (internal), registered as bundled
  resources.
- `.kanna/agents/pr-triage/AGENT.md` and `.kanna/agents/pr-reviewer/AGENT.md`,
  registered the same way. The existing `pr-triage/EXTEND.md` now layers onto a
  real agent instead of sitting inert.
- **Engine change A**: `diff_base_ref` on `POST /v1/tasks`,
  `kanna_create_task`, and `kanna-cli task create --diff-base-ref`, plumbed to
  the pre-existing internal `stored_base_ref`. Absent, behavior is unchanged.
- **Engine change B**: the diff view opens in branch scope when the task's
  worktree is clean and working scope when it is dirty, decided by a new
  `git_worktree_is_dirty` Tauri command. A remembered per-task scope still wins
  and skips the probe; a remote task view keeps working scope.

Deliberately **not** built: phases 2-4 (ordering-heuristic and brief quality,
which are judged by using them, and the change map, which is desktop UI work
needing visual verification). The `setup` agent's review-scope question is also
still unbuilt — it is specified in the design and belongs with the phase that
exercises it.

## Scope

In:

- Inspect the current desktop and mobile diff/PR review flows and state, in
  code, and name what actually blocks review-without-GitHub.
- Propose a concrete review experience, built from Kanna's existing
  primitives (workflows, agent definitions, child-task fan-out, the diff
  tool), covering: which PRs to review and in what order, the per-PR
  workspace and its diff, and what the agent contributes to the human's read.
- Phased implementation plan with tradeoffs, non-goals, and testable
  acceptance criteria per phase.
- Write the design to a spec document in `docs/specs/`.

Out, deliberately:

- Phases 2-4 of the design, and the `setup` agent's review-scope question (see
  "Fourth directive"). Phase 1 is in scope as of that directive; before it,
  the task was design-only.
- Removing or altering the existing verdict bar, comment overlay, or ⌘D diff
  modal — `f1c3ca89` owns that decision.
- Changing workflow stage-advancement semantics. The `pr` stage's `approve`
  post and its merge-master handoff are untouched by the design; a PR review
  session is a separate activity that never touches a task's own workflow.
- Building shared/forge-free review state. `docs/specs/forge-independence.md`
  is parked behind a decision gate and this task does not open it.
- Mobile advance-stage parity (owned by `f1c3ca89`), and a mobile equivalent
  of this flow, which the redirected design names as deferred.

## Constraints

- Engine stays forge-neutral: no `review_thread`/`review_comment` tables, no
  forge data model in SQLite, no `/v1` review-thread endpoints. Forge behavior
  lives in user-space `.kanna/` agents and config
  (`docs/specs/native-review.md`, `docs/specs/merge-master.md`).
- Compose Kanna's existing primitives — workflows, agent definitions,
  child-task fan-out, the diff tool — rather than adding review UI or a review
  data model.
- Design must be bounded and phased so each phase is shippable and testable on
  its own.

## Done when

- `docs/task-specs/231ad8fc.md` (this file) exists and is true.
- Phase 1 of the design is implemented, with tests, and the repository's own
  suites pass.

## Verification

- `./kd test all` — passed ("Canonical local verification passed").
- `cargo test -p kanna-server` — 1255 passed, 0 failed.
- `cargo test -p kanna-desktop --lib` — passes once sidecars are staged. Before
  `./kd test all` staged them, ten `commands::mobile` server-lifecycle tests
  failed on their fixture launch; with sidecars present that lane is 60/60.
  Those failures were environmental, not caused by this branch.
- `apps/desktop` vitest — 1785 passed; `packages/core` — 264 passed.
- `pnpm exec tsc --noEmit`, `cargo clippy`, `cargo fmt --all` — clean.
- Desktop E2E `mock/diff-view.test.ts`, including the new review-child opening
  scope case — see the result recorded in the run summary.
- `docs/specs/pr-review-dispatch.md` exists, covering diagnosis, the proposed
  experience, the engine changes it needs, phasing, tradeoffs, non-goals, and
  per-phase acceptance criteria.
- Work is committed on this branch. Nothing is pushed, no PR is created, no
  stage is advanced. The session stops with the design ready for owner review.

## Decisions taken while writing the design

- The design document is a **new** spec, `docs/specs/pr-review-dispatch.md`,
  rather than a rewrite of `docs/specs/native-review.md`. Native-review's
  phase 1 is implemented and shipped, and this design does not replace it —
  the two cover different activities. A `Related:` pointer says so in
  `native-review.md`.
- The design requires exactly two engine changes and no schema change:
  exposing the already-internal fork-point/diff-base split as `diff_base_ref`
  on task creation, and defaulting the diff tool's first-open scope to
  `branch` when the worktree is clean. A third (recorded base outranking
  branch upstream) is proposed as hardening, not a prerequisite, because
  forking the child from a local ref avoids the trap.
- Both mechanics were verified empirically against real git rather than
  reasoned about: a worktree forked from `origin/<head>` inherits that
  upstream and diffs to nothing, while one forked from a local ref has no
  upstream and uses the task's recorded base.
