# Task 0f627587 — Stripe Slice-0 provisioning

Record Tampopo LLC's Stripe sandbox/test account `acct_1Swy1rI0Oqa4EKBj` and live account `acct_1Swxz4RSDDrR2YPq`, and make the remaining Kanna Cloud product and monthly-price provisioning a single idempotent command for either Stripe test or live mode.

## Scope and constraints

- Document the completed account step and the remaining restricted keys, portal publishable key, and webhook signing secret access. Per the owner's 2026-08-21 directive, the sandbox account's test secret key is for the first provisioning run and staging function config; using the live account's key for eventual production provisioning is a named human step.
- Add a `STRIPE_SECRET_KEY`-driven provisioning script for Kanna Cloud's JPY 500 and USD/CAD/AUD/EUR/GBP 500 monthly prices, using stable `cloud_monthly_<currency>` lookup keys. Support `--dry-run`; never print or persist the key; make no live Stripe calls in tests.
- Replace Checkout's hardcoded price-id configuration with lookup-key resolution at the mocked Stripe gateway boundary, keeping the monthly-only owner ruling and existing billing behavior intact.
- Cover absent creation, existing-object reuse, dry-run no-write behavior, and Checkout lookup resolution with tests.

## Done

The documentation and operator command agree, reruns create no duplicate product or prices, Checkout resolves the provisioned monthly price by lookup key, the functions tests and TypeScript checks pass, and the repository test suite is run or any unrelated failure is reported.

## Out of scope

Dashboard/key creation, webhook registration, live Stripe calls, annual pricing, production deployment, and broader billing changes.
