# Mobile preview guidance

Explain through the existing shared CLI/MCP guide catalog how agents make task dev servers and visual companions available in Kanna Mobile. Cover concrete setup, mobile discovery, lifecycle, and current transport/format limitations, grounded in the implementation.

Extend the existing guidance and its discovery surfaces; do not change preview behavior, mobile UI, or networking. Done when CLI and MCP expose consistent, discoverable instructions and focused guide checks pass.

Verification (2026-09-05): CLI/MCP focused guide tests, all 35 catalog tests, changed-crate Clippy (`--all-targets -- -D warnings`), formatting, and built CLI Markdown/JSON/tool-discovery checks pass. `./kd test all` passed the workspace lane, then stopped in the server suite: `review_run_binding_refuses_cross_task_pair_and_allows_concurrent_verdicts` returned HTTP 500 instead of 200 at `revision_status.rs:1250` (1,240 server tests passed). The same test binary passed that case when run alone; the full suite is not recorded as passing and its later lanes did not run. No unrelated server change was made. This is guidance-only; no rendered UI or preview behavior changed.
