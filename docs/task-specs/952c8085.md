# Task 952c8085: notify humans when task management blocks

## Terms

Owner directive, 2026-08-25 (chat, paraphrased in the task prompt): when task management needs a human, send an out-of-band mobile notification rather than leaving the escalation among routine watch re-arm output.

- Update the task-manager definition proportionally to call `kanna_notify_mobile` once on entry into each distinct human-only blocker, with the affected task id and enough context to act without opening the terminal.
- Cover only the definition's existing human escalation surface: exhausted revision budgets, unauthorized publish-shaped actions, STOP/conflicting architect verdicts, human-only machine or terminal blockers, uncertain close/restart choices, and merge handoffs missing review coverage.
- Require honest interpretation of delivery counts and reasons. Push-only delivery means absent or zero `lanDeliveredCount` is expected.
- When every watched task is already human-blocked and notified, report that the event loop is intentionally idle before re-arming it.
- Extend the existing `@kanna/core` bundled task-manager asset assertion, run `./kd test all`, and confirm `kanna_list_agents` still resolves the definition. No runtime behavior or new E2E test is required for this definition-only change.

Done means the bundled definition and its pinned-content test encode this contract, focused verification passes, and the canonical suite is run with any unrelated failure recorded.

## Follow-up candidate

Do not implement here: raising the 240-second maximum event-wait timeout, or implementing the push-based watcher designed in `docs/task-specs/703f1b0c.md`, would eliminate full model turns spent only re-arming the watch while all work is human-blocked.
