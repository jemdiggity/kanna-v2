# Repo explorer swipe navigation fixes

Fix the mobile repository explorer so every ended, cancelled, rejected, or interrupted edge gesture settles at rest or completes navigation, including no-history forward swipes and touches that interrupt an active settle animation. Make left-edge back swipes capture over file-preview WebViews while preserving mid-screen WebView scrolling, and animate preview-to-list back navigation like directory back navigation.

This is a JS-only change in `apps/mobile/src/screens/RepoExplorer.tsx` and its headless state/component coverage; do not bump the mobile runtime version or broaden the navigation feature. Done means the relevant tests and TypeScript checks pass and the owner-mandated 2026-08-23 iOS Simulator gate is completed: reproduce both defects before the fix, then inspect screenshots proving below-threshold and no-forward-history settling, preview edge-back mid-drag and completion, and ordinary directory back-swipe behavior.

Verification procedure: the simulator gate must use real split W3C pointer sequences—pointer down, several short spaced moves, screenshot while held, then pointer up and a settled screenshot—for release/cancel and end-state evidence. A temporary dev-only fixed-offset fixture may photograph the preview's layered mid-state only if needed, but must be removed before commit; real gestures remain required for settle evidence. This procedure was directed by the owner on 2026-08-23 after the initial XCTest single-move attempt failed.

The initial verification run was superseded by revision round 1 and is not current acceptance evidence.

Revision directive (reviewer, 2026-08-23): scope start-phase capture to left-edge touches over the file-preview body so ordinary directory/header taps, including Back, Close, and edge rows, remain reachable. Preserve directory move capture and all settle guarantees; extend component coverage, rerun the specified mobile tests plus `./kd test all`, and replace the verification status/evidence with revised-build Simulator results covering the original four swipe scenarios and full visible Back/Close hit areas.

Revision verification status: complete. The final responder predicate captures only left-edge touches below the measured file-preview body boundary; directory and header starts are not captured. Targeted component tests and mobile typecheck pass, `./kd test all` passes, and the final revised build passed all four required swipe scenarios plus both visible extremes of Back and Close. Every revised screenshot was inspected; settled views have no offset or gutter. Current evidence and exact gesture/tap coordinates are recorded in [2475a486-verification.md](2475a486-verification.md).
