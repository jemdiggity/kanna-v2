# Task da6b6800 — Architect: accounts, subscriptions, and the adoption gap

Goal: architect consultation (no implementation) answering what architecture
Kanna needs for self-serve accounts and Stripe subscription billing, the
Apple/IAP posture, entitlement enforcement, the adoption gap list, and
sequencing — per the owner's 2026-08-20 request to "come up with specs".

Scope: verify the repo's current state, then author the spec artifact
`docs/specs/accounts-and-billing.md` and record one verdict. No product code,
tests, config, or follow-up tasks.

Done means: the spec answers all six questions in the consultation prompt with
verified evidence, names owner decisions with defaults instead of stalling,
and the verdict is recorded via `kanna_complete_stage`.
