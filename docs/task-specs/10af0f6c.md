# Task 10af0f6c: keep Claude palette chrome out of derived content

## Goal

Ensure daemon-derived `waitingPromptSnippet`/`snippet` never contains Claude CLI composer or slash-command palette chrome, while the separately labelled composer surface remains readable when a palette is open.

## Scope and constraints

- Fix frame classification in `crates/daemon/src/headless_terminal.rs` by using the composer as a positional boundary; do not classify palette entries with provider-specific text regexes.
- Cover palettes with at least three rows, palettes that push the composer outside the eight-row status window, and chrome interleaved below the composer.
- Keep raw PTY transcripts, task-log labelling, persistence/schema, and downstream server flow unchanged.

## Done when

Focused regression tests and the existing composer guards pass, the daemon test suite passes, and Rust formatting and clippy are clean.
