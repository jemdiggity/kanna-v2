# Task 1095ba6d: staging portal checkout and verification fixes

## Goal

Make the web portal and Firebase checkout function share one request/response contract, complete the Stripe return flow, and make email verification advance automatically after verification in another tab.

## Scope and constraints

- Share checkout request, response, and validation definitions across the portal and function; remove the portal's duplicated checkout interfaces.
- Submit `{ plan: "monthly" }`, reject a null checkout URL without redirecting, and align portal routes with the function's `/billing/success?session_id=...` and `/billing/canceled` URLs. Surface `session_id` on the success page.
- Provision one `cloud_monthly` Stripe Price with USD as its default and manual JPY/USD/CAD/AUD/EUR/GBP amounts. Let Checkout localize by customer location, and deliberately deactivate the legacy `cloud_monthly_<currency>` prices while retaining them for subscription history.
- While the verification page is mounted, recheck every few seconds and on window focus, stop after success or unmount, retain the manual fallback, and navigate automatically when verified.
- Add contract and component coverage for request validation, the informational six-currency price list, null URLs, automatic verification, and idempotent Stripe catalog migration. Do not deploy; the manager will rerun sandbox catalog provisioning and redeploy the portal and functions after merge.

## Directive history

- Owner, 2026-08-22: superseded the original currency-picker requirement — “NO currency picker. Currency selection is Stripe-native: the buyer must not choose their currency, it follows their location.” The checkout contract therefore has no currency field, and the catalog migrates from six single-currency prices to one multi-currency price.

## Done when

The requested portal tests, portal build, and repository test suite pass, and the implementation plus this spec are ready for the next workflow stage.
