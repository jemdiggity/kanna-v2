# Task bbe96646: relay anonymous push path

## Goal

Implement phase 3 of the owner-approved anonymous push design so a LAN-paired,
signed-out phone can bind its own FCM token with a desktop-signed certificate,
and the paired signed-out desktop can publish notifications through the relay.

## Scope

- Add certificate-verified `POST`/`DELETE /push/pairings`, token rotation,
  desktop-initiated signed revocation, Firestore `anonymousPushPairings`
  storage, binding caps, stale-binding GC, and registration/publish limits.
- Add replay-safe challenge/response WebSocket authentication for an
  `anonymousDesktop` principal that may only publish mobile notifications;
  refuse invoke, tunnel, snapshot, and ordinary desktop routing.
- Wire phone-authoritative registration/refresh/revocation into the mobile
  pairing lifecycle and add the kanna-server's lazy signed-out relay session.
- Cover binding lifecycle, delivery, forbidden capabilities, unpaired
  registration, rate-limit refusal, and the signed-out server publish path.
  Record the true-device-push CI limitation in a dated coverage-gap note.
- Deploy the completed change to staging with `./kd cloud deploy`.

Phase 4 dual-identity/account-token union delivery, entitlement-policy changes,
and production deployment are deliberately out of scope. The owner-approved
push-only amendment excludes any LAN notification banner work.

## Revision 1 lifecycle requirements (review, 2026-08-25)

- Desktop trusted-device removal must durably remove local trust and enqueue a
  signed `anonymous_push_revoke`; the anonymous relay loop deletes that outbox
  entry only after the relay acknowledges it, so offline removals drain after
  reconnect.
- Mobile notification subscription/effect teardown only detaches listeners.
  Phone-authoritative revocation happens only when `removeManualMachine`
  removes that pairing, and all anonymous binding POST/DELETE work is
  serialized and reconciled so certificate refresh or effect replacement
  cannot let an obsolete DELETE remove the current binding.
- Integration coverage must exercise the server persistence/WebSocket/relay
  revocation path and deferred mobile HTTP ordering across add, removal,
  certificate refresh, and effect replacement.

## Revision 2 hardening requirements (review, 2026-08-25)

- Phone unpair always attempts certificate-only relay revocation, including
  before FCM token initialization, and retains local pairing trust until the
  relay acknowledges deletion so failures remain retriable.
- Bound both pairing POST and DELETE certificate work per IP, and consume the
  anonymous desktop publish limit before querying bindings, including
  no-binding attempts.
- Retire revocation outbox entries from obsolete anonymous identity namespaces
  locally after key rotation, while retaining current-identity entries until
  relay acknowledgement.
- Add integration coverage for those lifecycle/limit behaviors and for binding
  caps, slot-neutral refresh/rotation, and stale-binding garbage collection
  survival/removal rules.

## Revision 3 revocation acknowledgement requirements (review, 2026-08-25)

- A valid current or newer pairing certificate must revoke a binding created
  with an older certificate, including when the newer certificate's refresh
  POST never reached the relay.
- A stale certificate must not revoke a binding registered with a newer
  certificate, and the DELETE endpoint must refuse rather than acknowledge
  that retained binding so the phone preserves retriable local trust.
- Relay integration coverage must exercise both certificate-ordering cases
  across the HTTP and Firestore boundary; update mobile refusal/retry coverage
  only if its HTTP contract changes.

## Revision 4 deployment completion requirements (review, 2026-08-25)

- Deploy the current relay fixes from this revision worktree to staging only
  with `./kd cloud deploy --staging --relay`, verify the public health endpoint
  reports the matching commit, and do not touch production.
- Record the deployed commit, Cloud Build id, health result, and
  `./kd test all` result. This is deployment completion only: retain the
  existing relay/Firestore/WebSocket, kanna-server, and mobile coverage rather
  than changing the test suites.

## Revision 5 certificate registration ordering requirements (review, 2026-08-25)

- The review finding required pairing registration to refuse an older
  certificate, or an equal-issued-at certificate with a different signature,
  with retriable `409 stale_certificate`; the existing certificate and FCM
  token must remain unchanged. The exact current certificate may refresh or
  rotate its token, and a strictly newer certificate may replace the binding.
- The task manager's 2026-08-25 final-round scope correction made the invariant
  explicit: every anonymous pairing identity mutation accepts only the exact
  current certificate or a strictly newer one; stale or equal-issued-at,
  different-signature certificates cannot mutate or resurrect a binding.
- The covered mutation paths are initial POST registration, exact-certificate
  token rotation, newer-certificate refresh, certificate DELETE, authenticated
  desktop revocation, invalid-token reconciliation, and stale-binding GC.
  Revocation and reconciliation retain a tokenless certificate watermark until
  the maximum validity window for every older certificate has elapsed, so a
  later stale POST cannot recreate the binding.
- Relay HTTP/Firestore integration coverage must prove stale POST and DELETE
  refusal with retained token/certificate state, resurrection refusal after
  phone and desktop revocation, and watermark preservation through GC.

## Revision 6 final staging deployment (review and owner, 2026-08-25)

- Review found that staging still served `178d80344`, predating the final
  certificate-ordering implementation at `9c91d15d7`; the owner reopened the
  exhausted task with the directive "you seem to need it."
- Deploy the current relay implementation to staging only with
  `./kd cloud deploy --staging --relay`, verify the public health endpoint
  reports the matching relay commit, and record the deployment evidence here.
- This correction is deployment-only. Do not change production or widen the
  already-reviewed implementation and test surface.

## Source correction (task manager, 2026-08-24)

The architecture spec is not yet on `main`: read it from
`task-38025180:docs/specs/anonymous-push-notifications.md`. Its delivery
recommendation is superseded by the owner's push-only amendment: no LAN banner
and no `notificationId` dedupe. The anonymous credential design and phase-3
acceptance criteria are unchanged. Relay certificate verification must match
the merged phase-2 implementation at `0e4f1d115` (PR #1230), including its
canonical signing payload, rather than relying on prose alone.

## Constraints and completion

The phone remains authoritative for its FCM token; the relay must validate the
desktop signature, certificate expiry, and binding namespace. Anonymous access
is display-only notification publication, with bounded payloads and abuse
limits. Existing signed-in registration, authentication, and push behavior must
remain compatible. Work is done when focused TypeScript/Rust checks and the
required integration tests pass, the coverage-gap note is present, and staging
deployment succeeds without touching production.

## Verification

On 2026-08-24, the relay's complete Vitest suite (255 tests), the focused
mobile notification/model suites (290 tests), mobile and relay TypeScript
checks, kanna-server relay and relay-client Rust tests, `cargo clippy`, Rust
formatting, and diff whitespace checks passed. The dev mobile app was rendered
in the iPhone 17 Pro iOS 26.5 Simulator through `./kd dev up --mobile`; the
paired task screen had neither a LAN-delivery banner nor reserved banner space.
The screenshot is retained in the task's gitignored screenshot artifact
directory for the current workspace.

The relay-only staging deployment completed through
`./kd cloud deploy --staging --relay` from implementation commit
`b2e20a9ffe1493f09fc7fc8d0942c27c31584cdf`. Cloud Build
`f0241d72-26c0-44bf-91dc-c84fe192ef2e` succeeded, the staging VM restarted the
new container, and `https://relay-staging.kanna.build/health` reported
`status: ok` with commit `b2e20a9ffe14`. No production deployment was run.

On 2026-08-25, revision 1 added focused relay coverage for signed desktop
revocation, kanna-server coverage for persisted offline revocation and
anonymous reconnect delivery, and deferred mobile HTTP/lifecycle coverage.
The focused relay, mobile notification/controller, and kanna-server suites,
mobile TypeScript check, Rust formatting/clippy checks, and `./kd test all`
passed. The full gate's clean-clone dependency bootstrap timed out once during
a cold run; its focused retry passed, and the complete gate then passed on
rerun.

On 2026-08-25, revision 2 made phone unpair certificate-only and retriable,
bounded pairing DELETE and no-binding publication attempts, retired obsolete
anonymous revocation namespaces before relay connection, and added the
requested mobile/HTTP/WebSocket/Firestore persistence, cap, refresh, and GC
coverage. The focused mobile suite (214 tests), relay suite (65 tests),
kanna-server rotation test, mobile and relay TypeScript checks, Rust formatting,
and `./kd test all` passed. A strict `cargo clippy -p kanna-server --all-targets
-- -D warnings` additionally reached an unrelated existing
`empty_line_after_doc_comments` warning in `transfer_sidecar.rs`; that
out-of-scope file was not changed.

On 2026-08-25, revision 3 made certificate-only revocation order-aware: the
exact current certificate or a strictly newer certificate deletes the binding,
while a stale or equal-issued-but-different certificate receives a retriable
`409 stale_certificate` refusal. Relay HTTP/Firestore integration coverage
proves both the missed-refresh/newer-DELETE case and stale-DELETE retention;
mobile coverage proves the refusal remains a rejected revocation. The focused
relay suite (264 tests), focused mobile suite (215 tests), relay and mobile
TypeScript checks, and `./kd test all` passed.

On 2026-08-25, revision 4 deployed the relay-only staging target from commit
`178d8034471e9b8dfb34fc59e07c31dceadf14a0`. The first image build
(`4e7af6c1-80fd-4d79-8c51-500fdc948362`) succeeded but the VM update retained
the old container after Docker Hub returned a transient `502 Bad Gateway` for
the unchanged Caddy image. The canonical staging command was retried; Cloud
Build `ff3f0424-7185-4aea-b16d-2e15d93eca99` succeeded and the VM recreated
the relay container. `https://relay-staging.kanna.build/health` then returned
`{"status":"ok","commit":"178d8034471e","connections":2,"tunnelFlow":{"pauseCount":0,"resumeCount":0,"capRejectCount":0,"maxBufferedBytes":0}}`.
No production command or deployment was run.

The requested `./kd test all` verification passed in full on the same commit:
all product lanes, the complete `@kanna/kd` suite (53 files and 703 tests),
including `tools/kd/tests/cli.test.ts` (30 tests), and canonical Rust tests
passed. No test-suite or E2E coverage changes were made in this deployment-only
revision.

Final acceptance audit: the relay integration suite records certificate-backed
binding register/rotate/revoke (including certificate ordering) in the
`anonymousPushPairings` store, challenge-response desktop authentication,
anonymous publish delivery, payload caps, refusal of invoke/tunnel/snapshot,
invalid-certificate registration refusal, per-IP/per-desktop/per-token
rate-limit refusal, binding caps, and stale binding GC. Kanna-server coverage
records lazy signed-out publication and durable desktop revocation; mobile
coverage records phone-authoritative registration, rotation, and retriable
revocation. The dated true-device FCM → APNs coverage limitation remains
recorded in `docs/2026-08-24-anonymous-push-device-delivery-e2e-gap.md`. With
the matching staging deployment above, every task-3 acceptance item is
satisfied; no item is declined or left outstanding.

On 2026-08-25, revision 5 centralized certificate ordering for every anonymous
pairing identity mutation and retained tokenless ordering watermarks across
phone revocation, authenticated desktop revocation, invalid-token
reconciliation, and stale-binding GC. The exact-current/strictly-newer rule now
also prevents stale POST replacement and resurrection after a newer binding is
revoked or retired. Relay TypeScript and the focused HTTP/Firestore integration
suite (64 tests) passed on the final diff.

The requested `./kd test all` run cleared 16 of 17 package lanes, including the
complete relay suite and every branch-relevant lane, but `@kanna/kd` ended at
702/703 tests when its untouched clean-clone fixture failed to remove its own
temporary directory with `ENOTEMPTY`. Two retries reproduced the same unrelated
temporary-directory cleanup race, once in the concurrent cold-launcher case and
once again in the clean-clone bootstrap case. No KD or companion/KSP code was
changed for this relay-scoped revision.
