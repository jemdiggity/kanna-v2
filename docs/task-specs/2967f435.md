# Task 2967f435: Mobile task dev-server preview

## Goal

Let Kanna Mobile open a task's declared, loopback-bound local web dev server when the phone is connected to that desktop over LAN.

## Scope

- Expose claimed task ports in mobile task detail.
- Add an authenticated, short-lived desktop LAN preview session that forwards one declared task port without rebinding the dev server to the LAN.
- Add the mobile Preview affordance and constrained in-app browser, including not-running and retry behavior.
- Cover the server/client wiring and security boundaries with automated tests.

Off-network relay preview, public/shareable URLs, undeclared ports, HTTPS upstreams, response-body rewriting, and Android-specific network configuration are out of scope. The relay design remains the P2 slice in `docs/specs/remote-dev-preview.md` and requires its recorded owner/infrastructure decisions.

## Constraints

- The preview may connect only to `127.0.0.1` at a port already claimed for that task.
- LAN access must use existing paired-device or loopback authentication; preview requests themselves use an independent enter secret followed by an HttpOnly cookie.
- Do not expose or rebind the user's dev server directly to the LAN.
- Mobile changes remain JS-only and do not bump `runtimeVersion`.

## Done

A paired mobile client can discover a task's declared port, open a live HTTP preview through the desktop, refresh/retry it, hand the same live listener to Safari with a fresh one-time entry path, and receive a clear not-listening state; unauthorized, uncookied, closed-task, and undeclared-port access is refused, and task/session teardown revokes the preview.

## Verification

Automated server tests cover paired authorization, enter-cookie exchange and reissue, HTTP and WebSocket forwarding, unknown-port and closed-task refusals, and refusal after a port claim is released. Mobile tests cover LAN routing, URL construction, task-detail propagation, controller capability gating, modal teardown, port switching, WebView errors, retry, Safari handoff, and the constrained WebView.

### Simulator attempt (2026-09-03)

The documented command starts the isolated desktop/server and Metro stack:

```text
$ ./kd dev up --mobile
Started tmux session 'kanna-task-2967f435-3'.
Profile: build=dev, owner=worktree, cloud=emulators.
Desktop owner: worktree (http://172.31.32.102:48126).
```

The dev client then failed to compile for the available iPhone XR simulator:

```text
$ KANNA_APP_ENV=dev pnpm --dir apps/mobile ios -- -d 88883097-9882-450B-A3A0-CCD0F49D27A5 --no-bundler
...
expo-modules-jsi/apple/Sources/ExpoModulesJSI-Cxx/include/RuntimeScheduler.h:61:26
error: 'RuntimeScheduler' cannot be annotated with either
SWIFT_RETURNS_RETAINED or SWIFT_RETURNS_UNRETAINED because it is not returning
a SWIFT_SHARED_REFERENCE type
...
CommandError: Failed to build iOS project. "xcodebuild" exited with error code 65.
```

After installing the pinned XCUITest driver in `.tmp/appium-home`, the scoped
smoke lane reached its app-install precondition and reported the consequence:

```text
$ APPIUM_HOME="$PWD/.tmp/appium-home" \
  KANNA_E2E_DESKTOP_SERVER_URL=http://127.0.0.1:48126 \
  KANNA_MOBILE_PORT=8098 KANNA_APPIUM_PORT=4729 \
  KANNA_IOS_SIMULATOR_NAME='iPhone XR' \
  pnpm --dir apps/mobile run test:e2e:smoke
Bundle build.kanna.app.dev is not installed on simulator iPhone XR.
Install it with: pnpm --dir apps/mobile ios -d "iPhone XR" --no-bundler
```

No Preview UI could therefore be launched, exercised, or screenshotted in the
simulator. The Preview action, fullscreen WebView, Refresh, Retry, multi-port
selection, Safari handoff, no-port state, and proxy-error/not-listening state
remain visually unverified in this worktree. No screenshot artifacts were
created. The dated gap note records the separate physical-device boundary that
a simulator cannot prove.

## Revision history

- 2026-09-03, reviewer feedback round 1: required Safari handoff to preserve
  the live WebView listener, added the named server/mobile coverage, and
  corrected the simulator verification path and evidence.
- 2026-09-03, merge-master feedback round 2: `pnpm test` failed the release
  Cargo-lock contract because `crates/kanna-server/Cargo.lock` and the Bazel
  crate-universe metadata did not list the new direct `hyper`, `hyper-util`,
  and `http-body-util` dependencies. Synced the server release lock through the
  `Cargo.server.toml` synthetic workspace (`--locked` passes) and refreshed
  `MODULE.bazel.lock` by Bazel evaluation. That same evaluation also rewrote
  the already-stale `kanna-cli`, `kanna-mcp`, and desktop release locks, which
  `main` had left unsynced after adding `tungstenite`; those are packaging-only
  and carry no product change.
