# PTY detail resize over mobile relay

## Goal

Ensure opening a PTY task detail on mobile sends its resolved authoritative terminal geometry (iPhone 17 Pro: 80x48) through the relay to the daemon.

## Scope and constraints

Trace and repair the product path from `TaskScreen` through navigation, mobile controller, relay/KSP, and daemon; add cross-boundary E2E coverage and verify in the simulator. Do not rely on fixture creation geometry as proof, and do not broaden the change beyond this regression.

## Done when

The detail mount produces the later 80x48 daemon resize snapshot in relay E2E, with the mobile UI checked in a simulator.

## Verification record

2026-09-05 revision 1: Manager bd122e9f clarified that the terminal-key receipt
fixture was accidentally carried from a separate key-strip journey whose product
commit is intentionally withheld for owner on-device testing. This task keeps
the relay setup/cloud-id repair and the raw-KSP post-mount 80x48 assertion, but
removes the unneeded terminal-key tracing wiring so this branch does not claim
coverage for absent product behavior. Required focused and full-suite results
are recorded after this revision's commands complete.

`pnpm --dir tests/remote-e2e exec vitest run src/scriptedAgent.test.ts
--maxWorkers=1` passed (10 tests). `KANNA_IOS_SIMULATOR_NAME='iPhone 17 Pro'
./kd test remote-e2e --dev --mobile-relay` passed, retaining the journey's
post-mount raw-KSP 80x48 assertion. `./kd test all` passed.

2026-09-05: focused mobile navigator tests, mobile TypeScript checking, and remote-E2E TypeScript checking pass. The strengthened relay E2E assertion was initially added but not executed because task 182f8d59's relay harness repair was still required before that lane could run.

`./kd mobile run --simulator` was retried on the assigned iPhone 17 Pro (iOS 26.5, UDID `C48044E2-D11B-4A50-993D-D571CA8462E7`). The retry cleared and regenerated the native project, completed CocoaPods, built, and installed Kanna Dev (`build.kanna.app.dev`) without repeating the earlier `ENOTEMPTY` cleanup collision. This change has no rendered-pixel change; the simulator launch confirms the updated build installs normally.

After manager bd122e9f reported the repair complete, this branch took only its harness commits: `23543b669` (as `df0a2857b`), `743c7c6b6` (as `4a6ebffa3`), and `42e33be49` (as `0c6b69e3d`); no product commits from that branch were taken. The merged fixture deliberately starts at 80x24. `KANNA_IOS_SIMULATOR_NAME='iPhone 17 Pro' ./kd test remote-e2e --dev --mobile-relay` then passed on the iOS 26.5 iPhone 17 Pro (`C48044E2-D11B-4A50-993D-D571CA8462E7`). Its raw KSP observer was armed before the first detail mount and passed only after receiving a new 80x48 snapshot, proving the mounted product resize rather than fixture seeding.
