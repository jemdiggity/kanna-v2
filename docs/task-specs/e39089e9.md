# Sidebar activity source-of-truth verification

## Goal

Establish with code-path and runtime evidence whether the desktop sidebar activity indicator and `task.activity_changed` event feed share one authoritative state, and whether the pre-0.3.0-staging.5 stale-until-click symptom is absent from the currently served staging build.

## Scope and constraints

- Trace Sidebar → desktop store/client → server/DB and daemon activity → server/DB → task-event feed, naming exact paths.
- Exercise a real working-to-idle transition without opening/refetching the task; compare before/after the e463bb8a fix if needed.
- If another desynchronization path exists, repair the shared source of truth and add cross-boundary E2E coverage. Do not add polling or click-triggered refresh workarounds.
- If the paths are unified and the symptom does not reproduce, deliver a dated audit note with the source, dev-runtime, and shipped-staging evidence. Do not claim staging verification from source or dev evidence alone.

## Done

The repository contains an accurate audit note and reproducible evidence for the conclusion. Any discovered wiring gap is fixed and tested, otherwise no production behavior is changed.
