# Task a0604117

Remove the stale `COPY patches/` instruction from the relay Dockerfile now that the workspace has no pnpm patches, so the relay image can build from the repository root again.

Keep the change one-line-scale and limited to dead patch/build-context assumptions. Verify `docker build -f services/relay/Dockerfile .` when Docker is available; the manager will perform the staging relay deploy after merge.
