# Remove the ⌘Z shortcut

Remove the desktop app's ⌘Z binding and its shortcut documentation/discovery entry. Preserve the underlying undo-close behavior and other ways of invoking it.

Done means ⌘Z is no longer recognized as a Kanna app shortcut, the shortcut help and product documentation no longer advertise it, and focused shortcut tests pass.

Visual verification (2026-09-02): started the real worktree app with `./kd dev up`. The shortcut-help modal no longer lists Undo Close/⌘Z, while the command palette still lists Undo Close without a key badge. WebDriver also confirmed a synthetic ⌘Z event is not handled. Inspected screenshots are stored in the gitignored `docs/task-specs/b3203116-screenshots/` directory.

Verification: 35 focused Vitest tests and both desktop TypeScript checks pass. The new mock desktop E2E scenario passed; its containing 20-test file completed 19/20 because the unrelated existing cloud-only selection test projected `[null]` instead of its remote task fixture.
