# Task 6280a277

## Goal

Fix product-review finding #5 so paired, trusted LAN access supports the same
task-file preview and file-mention resolution as authenticated relay access,
without making existing local features depend on the relay.

## Scope and constraints

- Trace and update mobile route selection, LAN transport, and server task-file
  authorization; preserve account/manual pairing, authorization, identity,
  path/symlink confinement, size limits, relay support, and unpaired rejection.
- Add meaningful unit/integration coverage for local-only, cloud-disabled,
  hybrid routing, credential validation, authorization/file boundaries, and
  mobile-client/server wiring; add a dated gap note if full E2E is not feasible.
- Audit related local feature paths and document broader relay-only findings;
  do not add cloud features or change billing/automatic pairing policy.
- No native/runtime changes, deployment, push, PR, or stage advancement.

## Done when

Paired trusted LAN file previews and mention resolution work with relay disabled,
relay remains supported, invalid/missing credentials and unauthorized paths are
rejected, tests and requested TypeScript/Rust checks pass, and this spec matches
the committed implementation.

## Audit

The related task directory/range and task-diff routes already accept paired LAN
access. The only unjustified relay-only gates found were task-file content and
mention resolution; account-scoped notifications and cloud publication remain
relay/account features and are unchanged.
