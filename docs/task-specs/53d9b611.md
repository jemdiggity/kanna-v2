# Task 53d9b611

## Goal

Preserve mobile task-composer input and surface the real delivery outcome when sending short, long, multiline, or dictated text through LAN/relay transports.

## Scope

Trace and fix the existing send path from native dictation/TextInput through task identity routing, transport, server queue/ledger, and daemon submission. Preserve held-by-draft queue semantics and attachment ownership; do not redesign the composer, resend uncertain deliveries, modify live staging data, or change unrelated worktrees/configuration.

## Done when

Known rejection, timeout, or disconnection keeps the submitted text visible and reports a relevant status; successful/queued delivery reports completion distinctly from daemon acceptance without CLI processing; focused tests cover outcome propagation and practical dictation/native state, plus required UI verification is recorded for owner testing.

## Verification record

The mobile Vitest suite and TypeScript check pass. `./kd mobile run --simulator` was attempted on the iPhone 17 Pro simulator but could not build because the installed `expo-modules-jsi` dependency fails Xcode compilation in `RuntimeScheduler.h`; simulator visual inspection therefore remains for owner/on-device testing once the build environment is corrected.
