# Task cf2000e8 — rendered mobile composer verification

## Goal

Verify the existing mobile task-input outcome UI from tasks 53d9b611 and
1b615769 on the iPhone 17 Pro simulator, then hand it to the owner for manual
on-device review. Do not re-implement that feature.

## Scope and constraints

Exercise delivered, held/queued, failed, and uncertain outcomes, including
draft/attachment retention and stale-result protection where practical. Use
only this worktree's isolated dev instance and a throwaway fixture task; do not
touch staging data, push, open a PR, or advance the workflow. Fix only rendered
defects found during verification.

## Done when

Screenshots for each exercised state are saved under this task's gitignored
screenshot directory, the checks pass, the verification record below describes
what each image shows, all processes started for verification are stopped, and
the combined branch is committed for the owner check.

## Verification record

References: [53d9b611](53d9b611.md) contains the implementation contract and
[1b615769](1b615769.md) records the pairing/build handoff.

- `delivered.png`: the composer shows the blue-gray check card, “Input accepted
  by the desktop; agent processing is not confirmed yet.”, and clears to
  `Reply…`.
- `held-queued.png`: after a real desktop W3C key action marked the draft as
  typed, the composer shows the amber clock and the full queued message; the
  mobile composer is cleared.
- `uncertain-mid-send-4.png`: stopping the recorded `kanna-server` PID during
  the send shows the red warning card with the connection error and preserves
  `uncertain test` in the composer.
- `failed-preserved.png`: the required `./kd dev down` attempt rendered the LAN
  route-unavailable overlay and the durable “awaiting delivery confirmation”
  banner. The route disables the composer before a local failed outcome can be
  formed, so a failed-card state was not provokable in this environment; it is
  not being represented as one.

No rendered defect was found and no product code was changed. The paired
fixture task was `3613e91d` on the disposable local server. Verification passed:
`pnpm --dir apps/mobile exec tsc --noEmit` and the focused TaskScreen,
`mobileController`, and relay task-flow suites (6 files, 358 tests).
