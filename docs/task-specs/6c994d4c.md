# Task 6c994d4c: desktop production spawn inventory test flake

## Goal

Reproduce and eliminate the failure in `production_spawn_path_publishes_identity_for_kd_cleanup`, making the production-path daemon inventory/cleanup coverage deterministic alone and under full-suite load on any development machine.

## Scope and constraints

- Trace the real desktop spawn path and kd process-inventory cleanup boundary; fix production code only if reproduction shows a real defect.
- Make test dependencies self-contained or skip loudly with a documented reason when they genuinely cannot be provided.
- Do not use sleeps or retries to mask synchronization or machine-state dependencies.
- Ignore the known load-sensitive `failed_replay_is_single_flight_and_backed_off_for_all_waiters` daemon flake.
- Unify `kanna-desktop`'s normal and test `tokio-tungstenite` dependency versions with no unrelated dependency bumps, because the duplicate direct dependency prevents the focused and full Rust test lanes from compiling. Added by manager directive on 2026-08-20: the clean-main `E0464` failure is in scope and must be fixed to complete verification.

## Done when

The focused test passes alone, the relevant environment dependency/root cause is documented by the implementation, and `./kd test all` passes (apart from the explicitly excluded known daemon flake, if encountered).
