# Task e66cdf77: Hide checkout reference

## Goal

Make the web portal checkout success page a plain human confirmation that the subscription is active, with a link back to `/account`, without displaying the Stripe Checkout `session_id`.

## Scope and constraints

- Keep `session_id` in the Stripe redirect URL and the portal's route/query handling; only remove it from rendered UI.
- Update affected component tests. Do not change checkout behavior, routing, or cancellation UI.
- Verify with `pnpm --filter @kanna/web-portal test` and the portal build.

## Done when

The success page confirms the active subscription and links to `/account`, no checkout reference is rendered, affected tests pass, and the portal builds successfully. Staging requires a portal redeploy after merge, handled by the manager.
