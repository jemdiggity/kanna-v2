# Task c21c9405: disjoint mobile swipe action

## Goal

Fix `SwipeableTaskCard` so swiping cannot crash from mixed animation drivers and pin/unpin/dismiss actions never render through or on top of card content, including when iOS Reduce Motion is enabled, while retaining the solid idle-to-armed color and scale treatment from #1204.

## Scope and constraints

- Confirm the reported Reduce Motion failure and inspect the ordinary-motion path in an iOS Simulator render.
- Reproduce the Reduce Motion OFF swipe-to-pin crash caused by native-driven scale and JS-driven color sharing one animated node, then separate the animation drivers while preserving 60fps motion.
- Use a reduced-motion animation that keeps card and action in disjoint horizontal space; action labels must never share pixels with card text.
- Extend structural tests where practical, especially preventing reduced row opacity over a visible action.
- This is JS-only: do not change mobile `runtimeVersion`, publish, push, or open a PR.
- Owner directive delivered 2026-08-23: “Let's test UI changes before making PRs.” Simulator screenshots are mandatory before any PR.
- Owner scope addition delivered 2026-08-23: Reduce Motion was enabled on the affected device, and after disabling it swipe-to-pin crashed; crash reproduction and crash-free completion in both motion modes are mandatory and take priority over the overlay fix.
- Revision feedback delivered 2026-08-23 clarifies that the screenshot gate must explicitly cover the owner's original pinned-card `Unpin` mid-swipe, armed, and released states with Reduce Motion both off and on, not only swipe-to-pin.

## Done when

Tests and TypeScript checks pass, the pre-fix ordinary-motion crash has been reproduced, and iOS Simulator screenshots for both `Pin` and the original pinned-card `Unpin` mid-swipe, fully armed, and released states with Reduce Motion both off and on have been visually inspected to confirm crash-free completion, disjoint card/action geometry, non-colliding text, and solid colors.
