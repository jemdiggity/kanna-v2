# Task 36a85e80 — Japan anti-fraud declaration

## Goal

Make the Stripe onboarding declaration verifiably true by explicitly requesting
EMV 3-D Secure in Checkout and recording the anti-fraud controls used by Kanna.

## Scope and constraints

- Set Stripe Checkout card options to `request_three_d_secure: "any"` and test
  the mocked session-create payload without live Stripe calls. `"any"` is used
  instead of `"automatic"` so 3DS is explicitly requested rather than left to
  Stripe's optimization; it can add authentication/challenge friction for card
  payments, including payments outside Japan, while Checkout owns the flow.
- Document the Japan Installment Sales Act / METI EMV 3DS context. Per the
  manager-relayed owner clarification on 2026-08-21, the onboarding declaration
  was exactly (1) EMV 3-D Secure (本人認証) and (2) IP-based fraud detection
  (不正検知 / 属性・行動分析 using suspicious-IP signals). Map the latter to
  standard Stripe Radar's IP-related risk signals and record Checkout CVV
  verification as an additional employed control, not one of the declared two.
  Include an operator step to verify Radar and its applicable default IP-risk
  protections on the live account.
- Radar for Fraud Teams and physical-goods shipping-address verification are
  out of scope. No client changes, production deploys, or live Stripe calls.

## Done when

The gateway sends the explicit 3DS option, mocked tests pin that Stripe payload,
the billing spec records the declaration and dashboard runbook, and the
Firebase Functions suite plus repository `pnpm test` pass.
