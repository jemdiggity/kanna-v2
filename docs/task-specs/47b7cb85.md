# Task 47b7cb85: refuse unresolved task bases

## Goal

Make task worktree creation resolve the repository default branch explicitly and fail with an error naming that branch when neither `origin/<default>` nor local `<default>` exists, instead of letting `git worktree add` fall back to the primary checkout HEAD.

## Scope

- Prefer the remote-tracking default branch when available, including after a pre-creation fetch that can refresh or discover it; otherwise use the local default branch.
- Cover task creation when only `origin/<default>` exists and when the recorded default exists nowhere.
- Do not change repository registration/default-branch persistence (task `f36897c4`) or ahead/behind reporting (task `86f10a2c`).
- Coordinate with task `86f10a2c`'s neutral `git_refs` resolver rather than adding a duplicate. Its 2026-09-03 directives established the API as `resolve_base_ref(&Path, &str) -> Option<ResolvedBaseRef>` and require creation to consume `reference`; that field must remain available even when no merge-base with the primary checkout's `HEAD` exists.
- The companion's 2026-09-03 shared-resolver commit `999d7ba1a` is a stacked dependency of this task. Its follow-up `4cad82799` confirms the same option-terminator compatibility fix applied in this task's layer (`--end-of-options` for `rev-parse`, `--` for `merge-base`).

## Done

Task creation uses a validated start point, refuses an unresolvable recorded default branch with a branch-specific error, and the relevant end-to-end/integration tests pass.
