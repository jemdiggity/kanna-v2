# Task 97f63d2f: correct installer release repository

## Goal

Make `scripts/install.sh` fetch Kanna releases from the canonical `tampopogk/kanna` repository and prevent its repository constant from drifting away from the origin remote.

## Scope and constraints

- Confirm the installer's latest-release lookup, release tag, DMG name, and download URL against `kd release ship`, the release runbook, and the latest real GitHub release.
- Correct stale `jemdiggity/kanna` references only when they are current install documentation or help for this same repository.
- Add a cheap automated test tying the installer's `REPO` value to the origin repository; update an existing install test if one exists.
- Leave these audited `jemdiggity` references unchanged:
  - `apps/desktop/src-tauri/tauri.conf.json`, `apps/desktop/src-tauri/BUILD.bazel`, and `apps/desktop/src/updater.test.ts` are updater endpoints and their pinned test; updater configuration was explicitly excluded from this installer correction.
  - Root/crate/package Cargo manifests and lockfiles, `MODULE.bazel`, `MODULE.bazel.lock`, and `vendor/libghostty-rs/**` name pinned `ghostty`, `libghostty-rs`, `ghostty-xterm-compat-serialize`, or `rules_tauri` dependency forks rather than this repository's release location.
  - `apps/desktop/src/**/*.test.ts`, `apps/desktop/src/tauri-mock.ts`, and `tools/kd/tests/*.test.ts` use old-owner URLs as fixtures, examples, or negative regression strings; release behavior derives its repository dynamically from the supplied remote.
  - `docs/superpowers/**`, `docs/2026-08-07-cross-version-fixture-network-e2e-note.md`, and `docs/task-specs/d66c4550.md` are historical plans, designs, incident notes, or another task's durable record.
- No UI behavior changes and no simulator verification.

## Done when

The installer uses `tampopogk/kanna`, its derived asset URL matches a real asset in the latest release, no unintended stale owner references remain, the drift test exists, and `./kd test all` passes.

## Validated release contract

`kd release ship --release` tags `vX.Y.Z` and publishes `Kanna_X.Y.Z_arm64.dmg` and `Kanna_X.Y.Z_x86_64.dmg`. When checked on 2026-09-03, GitHub's latest `tampopogk/kanna` release was `v0.2.0` and exposed both corresponding DMGs; the installer uses the same tag stripping and architecture labels and does not fetch separate checksum or signature files.
