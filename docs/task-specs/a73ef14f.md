# Mobile swipe-action visual polish

Polish `SwipeableTaskCard` so pin, unpin, and dismiss pills remain solid and confident throughout a swipe, using color and a restrained scale change—not mid-range whole-pill transparency—to communicate the armed state.

Preserve the existing reduced-motion and draw-only accessibility behavior. This is a JS-only, OTA-deliverable change: do not bump the mobile `runtimeVersion`.

Done means focused tests prove mid-swipe pill opacity is at least 0.95 for every variant and crossing the commit threshold changes color/scale; manually sanity-check the animation values and run the relevant TypeScript/tests.
