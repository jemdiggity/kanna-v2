# Task 8275f44c: staging release version safety

## Goal

Prevent `kd release ship --staging` from publishing a semantic-version rollback and make a bare staging ship continue the unpromoted series already served by `desktop-staging`.

## Scope and constraints

- Compare the fully derived candidate version with `latest-staging.json` before building or publishing; require a strict semver increase, including prerelease ordering, with an actionable refusal.
- A bare staging ship continues the active unpromoted `X.Y.Z-staging.N` series as `N+1`; explicit `--minor` or `--major` starts a newly derived series. Preserve release-branch behavior and explicit rollback tooling.
- Cover regression refusal, automatic continuation, and explicit new-series derivation with mocked-channel dry-run tests, including the CLI's nonzero refusal exit convention.
- Update release-candidate documentation. Do not publish, promote, or reset any release channel.

## Done when

`pnpm --filter @kanna/kd test` and `./kd test all` pass with the new release-tooling coverage.

## Revision 1

On 2026-08-21, the task manager relayed the reviewer's dropped verdict: add the missing CLI-level test proving a staging semver refusal returns a nonzero exit code. This revision is limited to that coverage; the release implementation is unchanged.

## Revision 2

The revision prompt contained no reviewer feedback, and the delivered-input ledger contained no newer directive. The Revision 1 nonzero-exit coverage remains the complete recorded assignment for this branch.
