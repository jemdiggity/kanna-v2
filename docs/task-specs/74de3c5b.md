# Task 74de3c5b: deterministic kd launcher-test cleanup

## Goal

Stop the intermittent `ENOTEMPTY` false failure in the `@kanna/kd` lane's
"serializes concurrent cold launchers and serves MCP from the shared install"
test by fixing the process-lifecycle race before its temporary directory is
removed.

## Scope and constraints

- Trace and deterministically await every process started by the test before
  teardown removes its fixture; do not mask the race with broad retries,
  `force`, or `maxRetries` changes.
- Keep the change confined to this test and the narrow lifecycle behavior it
  exercises; do not rework the kd test harness or adjacent cleanup code.
- Preserve the test's concurrent cold-install and MCP behavioral assertions.

## Done when

The affected test passes repeatedly, several consecutive `@kanna/kd` lane
runs pass, TypeScript checks pass, and `./kd test all` is run with the exact
results recorded in the task summary.
