# Task 1b615769 — continuation of 53d9b611

## Goal and scope

Carry forward `origin/task-53d9b611` at `bd49b6dfdbc3c14de3fd231f10126d2b8258a545` for review: verify its mobile composer delivery-outcome UI on an iPhone 17 Pro iOS 26.5 simulator, run the required checks, and record the owner-check handoff. The implementation terms remain in `docs/task-specs/53d9b611.md`; this task does not re-implement or widen that work. Per manager `bd122e9f`'s 2026-09-05 directive, verification may create and close a throwaway task only in this worktree's isolated dev instance; it must not touch staging data.

## Done when

Rendered states (delivered, queued/held, failed, uncertain, attachment retention, and stale-result protection) are exercised with screenshots and recorded below, checks pass, processes are stopped, and the combined branch is committed for manual owner on-device review. No push, PR, or workflow advancement is in scope.

## Verification record

On 2026-09-05, `./kd mobile run --simulator 'iPhone 17 Pro'` built, installed, and launched the dev app on iPhone 17 Pro `C48044E2-D11B-4A50-993D-D571CA8462E7` (iOS 26.5), paired to this worktree's dev desktop endpoint. `initial.png` shows the real rendered paired Tasks screen. The initial dev database had no task rows; the manager then clarified that an isolated throwaway task is the intended verification fixture. I registered the worktree only in its dev server and created fixture task `7af47e5e` there.

The subsequent canonical mobile launch reinstalled the dev app and cleared its pairing. I created a real local pairing session, but `simctl openurl` cannot route the test-only pairing payload into the dev app: `kanna://e2e-pair` is unregistered and `kanna-dev://e2e-pair` opens the app without consuming the payload. `fixture-list.png` records the resulting unpaired screen. The state-by-state UI check therefore remains blocked until the pairing sheet is driven interactively or via Appium's `mobile: deepLink` path. I stopped the dev stack and simulator; fixture task `7af47e5e` was left only in the isolated worktree dev database after a cleanup close attempt raced the server shutdown.

Manager follow-up: Appium 2.19.0 with XCUITest 9.9.1 started on this task's `KANNA_APPIUM_PORT` (4756). The initial selector failure was navigation, not a missing field: tapping `mobile.tasks-pair-mac` opens the Machines screen, and the smoke spec's next control, `mobile.machines-add`, opened the sheet and exposed `mobile.machine-pairing.code` as expected. `paired-result.png` records the rendered sheet with the input and its concrete response: the first session code had expired while WDA/Appium started. A fresh pairing session must be created immediately before entering the code; no product/UI defect was found.

One-pass retry: the automation reached the code field first, then created a fresh `/v1/pairing/sessions` response and submitted it immediately. `fresh-pairing-result.png` shows the successful paired machine row (Gu’s MacBook Pro, `PAIRED`, available on this network). This proves the required fresh-install pairing path. Composer-state verification remains to be resumed from this paired fixture.

Focused coverage passed: `pnpm --dir apps/mobile test -- src/screens/TaskScreen.attachment.test.tsx src/state/mobileController.test.ts e2e/specs/relay/relay-task-flow.test.ts` (263 tests), and `pnpm --dir apps/mobile run typecheck` passed. The literal root `pnpm exec tsc --noEmit` cannot typecheck this repository because the root has no `tsconfig.json` (it prints the TypeScript help); no product TypeScript error was observed in the mobile project check.
