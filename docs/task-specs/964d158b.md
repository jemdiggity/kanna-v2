# Task 964d158b

Fix `kd` so cloud-deploy prerequisites are scoped to the selected targets: relay-only staging deploys must not require portal environment variables, while portal-inclusive deploys must require them. Audit the other cloud targets for the same prerequisite leakage.

Ensure every refused `kd` invocation (including unknown flags, missing prerequisites, and failed preconditions) exits nonzero by correcting the shared CLI exit path rather than individual messages. Add coverage for relay-only deploys without portal variables, portal-inclusive refusal with nonzero status, and unknown flags with nonzero status.

Owner directive, 2026-08-21 00:20 JST: make portal deployment idempotently ensure its configured Firebase Hosting site exists before deploying, creating an absent site and propagating provisioning failures as nonzero exits. Cover this at the mock/dry-run level; the staging site was created manually and must not be used as test state.

Done when `pnpm --filter @kanna/kd test` and `./kd test all` pass, subject to locally available external prerequisites.
