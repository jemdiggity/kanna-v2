# Portal root Firebase injection fix

Fix the staging web portal white screen by giving `App.vue` direct access to the portal Firebase singleton while preserving `usePortalFirebase()` as the descendant injection contract.

Add a cold component test that mounts `App.vue` with a router and proves the sign-in screen renders without throwing. Keep existing page tests unchanged. Verify the portal test suite, repository test suite, production portal build, and a local preview smoke. Deployment is out of scope; staging must be redeployed by the manager after merge.
