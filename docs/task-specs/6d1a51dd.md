# Task 6d1a51dd

## Goal

Implement product-review finding #3: make post acceptance and new stage-run
creation crash-consistent through durable operation intent/identity and startup
reconciliation owned by `kanna-server`.

## Scope

In scope: the server/DB lifecycle paths for in-session posts and fresh stage
workspace runs, their completion bindings and recovery consumers, plus focused
fault-injection/restart E2E coverage for refusal, unknown acknowledgement, and
successful continuation. Preserve existing delivery/error semantics, fresh
workspaces, policies, and client surfaces. Out of scope: sidebar UX, client
polling/retry workarounds, deployment, and workflow advancement.

## Done when

Accepted posts cannot be duplicated or lose their durable run identity after a
crash; newly spawned runs cannot expose an agent with stale task/workspace
pointers; startup reconciliation repairs or safely resumes interrupted intent.
Tests reproduce both crash windows and verify restart reconciliation, including
known pre-submission refusal, unknown acknowledgement, and successful
continuation. Rust formatting, clippy, and relevant full checks pass.

## Revision terms (2026-09-05)

Reviewer feedback requires that interrupted cleanup rolls back only a freshly
forked workspace; a resumed revision's existing worktree and branch survive.
Committed post identity must remain successful if completion-context rebinding
only warns, and an uncertain delivery must be reconciled in-process with one
daemon presence query before a later post or stage operation is allowed.
