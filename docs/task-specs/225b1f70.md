# Firebase portal URL parameter

Owner directive (2026-08-21): migrate the public `KANNA_PORTAL_BASE_URL` from Secret Manager to Firebase Functions parameterized configuration backed by committed per-project `.env` files, including production/local defaults and staging. Keep emulator and test process-environment overrides working.

Scope is limited to the Functions billing configuration and bindings, deploy/config validation that covers those bindings, and the accounts-and-billing operator runbook. `STRIPE_SECRET_KEY` and `STRIPE_WEBHOOK_SECRET` remain secrets; no other billing behavior changes, and the obsolete Secret Manager value is documented for operator cleanup but is not deleted by this task.

Done means checkout no longer declares the portal URL as a secret, the parameter is required and available in deployed and emulator/test paths, committed environment values cover the Firebase projects, documentation reflects provisioning and cleanup, and the functions suite (including binding coverage) plus repository `pnpm test` pass.
