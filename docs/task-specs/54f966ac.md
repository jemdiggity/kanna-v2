# Task 54f966ac: mobile cloud-profile Firebase config

## Goal

Make the supported `kd mobile run --device --build dev --owner staging --cloud staging` profile bake the staging cloud Firebase JS configuration (and matching cloud-derived relay/OTA metadata) instead of the dev emulator placeholders.

## Scope and constraints

- Keep the dev native identity and its Firebase Messaging plugin/plist exclusion unchanged; this task affects JS/cloud configuration only.
- Keep development builds' existing disabled Expo Updates behavior; selecting staging cloud metadata must not enable staging OTA/signing in a dev native build.
- Keep `dev/worktree/emulators` on the intentional `kanna-local` placeholder configuration, and disable Firebase cleanly there so expected missing cloud credentials do not produce runtime errors.
- Add focused config-resolution/runtime tests. Do not change unrelated mobile identity or release behavior.

## Done when

- Config resolution selects staging Firebase and relay values for a dev/staging/staging profile, while emulator profiles retain placeholders.
- Intentional placeholder Firebase configuration does not initialize the cloud Firebase SDK when no emulator endpoints are configured.
- Relevant tests and TypeScript checks pass, and delivery requirements (OTA versus rebuild) are documented in the handoff.
