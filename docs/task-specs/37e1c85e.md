# Firebase hosting target coverage

Ensure the `account` hosting target declared in `firebase.json` is mapped for the local, staging, and production Firebase projects, and guard every declared hosting target across those three mappings.

Done means the kd configuration test, `./kd emulators exec -- true`, the remote-E2E emulator boot, and `pnpm test` pass. No hosting deployment or application behavior changes are in scope.
