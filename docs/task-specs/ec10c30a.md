# Firebase Admin initialization and reproducible functions deploys

## Goal

Resolve the 2026-08-22 staging `createCheckoutSession` failure where Cloud Run reached `getFirestore()` without a default Firebase app despite the lazy `getApps()` guard, identify the deployed-only mechanism with evidence, and replace the manager's deploy-local symptom hotfix with a deterministic fix on main.

## Scope and constraints

- Trace and reproduce the deployed dependency/runtime path rather than inferring the cause from the exception.
- Initialize Firebase Admin deterministically in the functions entry point while keeping credential-free unit imports working and duplicate initialization safe in tests.
- Pin the `services/firebase-functions` deploy dependency tree so Cloud Build installs the tree tested in the repository.
- Add regression coverage that loads the real compiled entry point in a credential-free child process, plus a dated E2E-gap note if CI cannot reproduce Cloud Run itself.
- Do not deploy; after merge, the manager must redeploy staging functions so main replaces the deploy-local hotfix.

## Done when

The root cause is evidenced and documented, initialization and deploy packaging are fixed, relevant tests and TypeScript checks pass, and the staging redeploy requirement is called out in the handoff.
