# Task 102ee880

## Goal

Determine whether the owner-observed Codex TUI input/output blending is caused by upstream Codex styling or Kanna's terminal rendering, using a current live interactive Codex session and the `tests/tui-fidelity` paths.

## Scope

In scope: a sanitized current capture with several turns, long and multiline input, settled output, paired screenshots/cell diffs, raw-byte replay through direct xterm and Kanna daemon headless snapshot/KSP/mobile rendering, relevant desktop rendering checks, existing fidelity checks, and durable findings. A narrowly demonstrated rendering/test fix may be made if evidence supports it. Out of scope: UI redesign, deploys, pushes, PRs, stage advancement, global `HOME`/`CODEX_HOME` changes, and fixture credentials or private conversation.

## Constraints and done

Use an installed inexpensive Codex model (for example `gpt-5.4-mini`) for live probing; do not use Astra for fixtures. Keep temporary captures/workspaces under `.tmp/`, screenshots under the gitignored task-spec screenshot directory, and stop every process started by this task using its exact PID/process group. Done means the tested paths, inspected screenshots/diffs, upstream-versus-Kanna conclusion, bounded fix proposal or implementation if warranted, and verification results are recorded in a durable findings document/spec update.

## Findings (2026-09-05)

- Live probe: `codex-cli 0.153.3`, interactive `codex --no-alt-screen`, `gpt-5.4-mini`, task-local `CODEX_HOME`, disposable `.tmp/live-probe`, three submitted turns. It included the `›` user composer marker, long wrapped dictated-like input, bracketed-paste multiline input with a blank line, settled replies, and Codex's `•` assistant/status marker.
- The sanitized capture is `tests/tui-fidelity/fixtures/codex-live-20260905.ansi`. The real daemon emitter produced two `term_snapshot` frames (initial attach and reconnect), eight `term_output` frames, 28,444 live-output bytes, and no `visible_text_vt` fallback at 120×36. Direct xterm versus KSP/mobile replay was 0 divergent cells; paired screenshots are in `docs/task-specs/102ee880-screenshots/`.
- Existing `tests/tui-fidelity` coverage also passed for live append, snapshot/reconnect, scrollback, dimensions, text/blank lines, foreground/background, bold/dim/inverse attributes, and prompt markers. The old `codex-pwd-tool` fixture is 0 divergent cells in this checkout; its README's former 59-cell report was stale.
- Desktop was exercised through the production terminal attach code's test suite (188 files / 1,780 tests passed) and `pnpm --filter @kanna/desktop build`; desktop and mobile both use `@xterm/xterm` 6.1.0-beta.195. No desktop renderer defect appeared in the tested attach path.
- Conclusion: the demonstrated ambiguity is upstream Codex presentation, not Kanna byte transport or cell rendering. Codex gives user text a bold `›` marker and assistant/status text a `•` marker, but does not provide a strong visual boundary or distinct background; Kanna preserves those bytes and the direct/mobile grids match exactly. No UI redesign or Kanna fix is warranted by this evidence. A future bounded product change would need to be an explicit, opt-in turn-separation presentation policy rather than a terminal-fidelity correction.
