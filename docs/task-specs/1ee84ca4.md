# Task 1ee84ca4

Fix the five non-blocking findings from the round-3 review of terminal scrollback capping: benign mobile handling of `no_scrollback`, reconnect-loop and registry eviction cleanup for idle terminal taps, the empty-chunk prepend-script shape, the remote-E2E waiter timeout leak, and the two noted cosmetic documentation/comment issues.

Add focused unit coverage for the mobile `no_scrollback` path, failing-connect tap eviction, and empty-chunk prepend script. Verify with `cargo test -p kanna-server` and `pnpm test`.

Do not change the accepted chunk/window seam SGR reset fidelity cost or otherwise expand the terminal scrollback design.
