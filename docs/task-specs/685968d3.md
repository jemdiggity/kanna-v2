# Mobile task composer soft-wrap auto-grow

## Goal

Make the mobile task reply composer grow and shrink for native soft-wrapped text as well as explicit newlines, up to the existing five-line cap with internal scrolling beyond it.

## Scope and constraints

- Preserve reset after send, attachments, keyboard avoidance, and bottom-anchored controls.
- Prefer native layout measurement; do not estimate wrapping from character widths.
- Add headless coverage for a zero-newline draft receiving a wrapped native content height.
- Verify one-line through capped, mixed-newline, delete, and send states in an iOS Simulator Release build, recording whether each screenshot contains explicit newlines.
- This is JS-only; do not change mobile `runtimeVersion`.
- Also amend `CLAUDE.md` so interaction/feel changes pause before review for human on-device testing and owner feedback stays in the same task.
- Owner scope addendum, 2026-08-23: add the same human-in-the-loop gate to the repository `implement` and `review` agent definitions; implementers stop at the manual stage after simulator verification, and reviewers require recorded human sign-off before reviewing UI-feel diffs.
- Owner process directive, 2026-08-23: after implementation, the full Release-simulator matrix, and a local commit, stop at the manual stage for physical-device approval; do not proceed toward review or PR. The owner said, “we need human-involved user-interface testing — the nuances are hard for agents to get.” Any resulting polish feedback must be iterated in this same task so the approved result ships as one PR.
- Owner on-device verdict, 2026-08-23 (iPhone 15, Release OTA from `766b8536`): soft-wrap growth passed, but overflow did not scroll or follow the caret, and dismissing the keyboard left the composer expanded. For the next hands-on pass, eight lines must keep the caret line visible while typing and allow scrolling back to earlier lines; keyboard dismissal must collapse the composer without clearing its draft, and refocusing must restore the draft's fitted/capped height. Re-run the Release screenshot/measurement matrix with these states, commit, and stop again at the manual stage without proceeding to review or PR.
- Owner on-device verdict round 2, 2026-08-24 (Release OTA `d0366910`): soft-wrap growth, the five-line cap, internal caret-following scroll, and keyboard-dismiss collapse passed. Refocus remained stuck at the collapsed height with the long draft unreachable. On focus, restore the retained or remeasured draft height immediately up to the cap, keep long drafts scrollable even while collapsed, place or preserve the caret in view so typing can continue immediately, extend the wrapped-draft focus/blur/refocus headless cycle, commit, and stop again at the manual stage for the owner's round-3 device pass.
- Review revision directive, 2026-08-24: the existing Release screenshots predate the refocus fix at `a3354f0d8` and are not final-code evidence. Re-run the complete Release-simulator matrix from the reviewer feedback against `a3354f0d8` or later, including zero-newline wrap and overflow, mixed wrapping/newlines, delete, send, eight-line caret/manual scrolling, keyboard-dismiss collapse, and immediate refocus regrowth with usable scrolling/caret visibility. Record fresh screenshots, measurements, explicit-newline status, and build identity; run the focused tests, typecheck, complete mobile suite, and `./kd test all`. After committing the final evidence and any required fix, remain at the manual stage until the delivered-input/task record contains the owner's round-3 physical-iPhone Release approval of refocus, collapsed scrolling, caret visibility, cap/overflow, shrink, and reset. If the device pass finds a defect, fix it and repeat the final-code matrix before asking again.

## Done when

The composer visibly grows for soft wraps, caps and follows the caret with usable internal scrolling after five visual lines, collapses with its draft intact when the keyboard is dismissed, restores its fitted height on refocus, shrinks on deletion, resets after send, retains the listed existing behaviors, and the focused tests, mobile typecheck, complete mobile suite, `./kd test all`, and fresh final-tip Release visual matrix pass. The work is committed locally and then held at the manual stage until the task record explicitly contains the owner's round-3 physical-iPhone Release approval.

Release-simulator measurements and inspected screenshots are recorded in [685968d3-verification.md](685968d3-verification.md).

Final-code simulator evidence, 2026-08-24: the complete iOS 26.5 `Release` matrix passed from Git tip `a3354f0d8`, including zero-newline growth/overflow and manual earlier-text access, mixed wrapping/newlines, shrink/reset, eight-line caret/manual scrolling, keyboard-dismiss collapse with retained draft, and immediate refocus to 120 points with visible caret and usable scrolling. The owner's round-3 physical-iPhone Release verdict is still required in the delivered-input/task record before review.
