# 5ded9d27 — Relay scaling plan: 10 → 100 → 1k → 10k users

## Goal

Owner request 2026-08-20: an architecture plan for scaling the relay from
today's single e2-micro VM to 10, 100, 1,000, and 10,000 users. Deliverable:
a committed spec at `docs/specs/relay-scaling.md`. Analysis and a staged plan
only — no implementation.

## Scope

In:

- `docs/specs/relay-scaling.md` answering, per stage: what breaks first (with
  arithmetic), the minimal change to survive it, the multi-relay
  routing/sharding design, operability gates (observability, load test,
  deploy story), and a cost table sanity-checked against the ~$10/mo
  subscription.
- `docs/task-specs/5ded9d27.md` (this file).

Out:

- Any change to product code, tests, deploy scripts, or configuration.
- Verification beyond markdown formatting conventions of `docs/`.

## Constraints from the prompt

- Treat as landed baseline the in-flight efficiency work: per-user byte
  odometer (task `2afa6564`), diff-aware task publication (`28dbd45b`),
  desktopCredentials auth cache + perMessageDeflate (`058e51d5`).
- Use the 2026-08-20 cost analysis figures (~$5–6/user/mo pre-levers,
  <$1 post-levers) and `docs/specs/accounts-and-billing.md` as billing
  context.
- Re-verify survey claims against source; keep stages concrete enough to
  become implementation tasks; flag owner decisions (spend, multi-region,
  managed-platform migration).

## Done means

The spec is committed with all five per-stage questions answered from
source-verified facts, GCP prices flagged as estimates to re-verify, and
owner-decision points called out.
