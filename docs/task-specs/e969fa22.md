# Task e969fa22

Rename the built-in task-manager command/template label from “Kanna Task Manager” to “Task Manager,” including its focused tests and future singleton launch title. Keep the `task-manager` slug unchanged and do not alter existing tasks’ stored titles.

Done when task template resolution exposes “Task Manager” consistently and its focused tests pass.
