# Task 7e0a0748

Clarify the bundled task-manager instructions that desktop releases must be executed through a shepherded Ship task, with direct manager intervention limited to machine-state blockers and any manual publish verified by `./kd release status` showing the channel version moved.

Briefly check the bundled Ship task template and Ship agent instructions, and state plainly there that `./kd release ship` requires `--release` to publish if that distinction is not already explicit. Keep the change doc-only, concise, and in the existing voice; do not restructure the instruction files. Verify bundled agent/template resolution with the relevant narrow tests.
