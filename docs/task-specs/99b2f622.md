# Task 99b2f622

Correct only the stale revision facts named in the task: update AGENTS.md's revision-resume bullet to describe provider support and fresh-fork conditions accurately, and change the shipped workflow schema's omitted `revision_limit` default from 3 to 5.

Verify the claims against the server implementation, check mirrored schema surfaces and `docs/`/`.kanna/` for other stale default-3 claims, and run the relevant schema validation. Do not otherwise edit AGENTS.md or `docs/dev/`.
