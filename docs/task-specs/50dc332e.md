# Task 50dc332e

Replace the mobile task composer's paperclip attachment glyph with a minimal, unfilled `+` at the leading edge of the composer row, following Slack's contextual attach-actions pattern. Preserve the attachment control's tap target and behavior, and label it `Attach photo`.

Scope is limited to the JS/TS mobile composer and directly affected component coverage. Do not change the screen-level floating `+`, attachment behavior, native configuration, dependencies, or any mobile `runtimeVersion`.

Done means the old paperclip is no longer rendered, tests assert the new icon and unchanged semantics, and `pnpm --filter @kanna/mobile test` passes.

Directive update: the owner clarified on 2026-08-21 (with a Slack screenshot reference) that the composer's control should specifically be a thin `+` and coexist with the screen-level `+`; this supersedes the original preference for a photo glyph when plus was already present. The owner also specified the exact action-specific accessibility label `Attach photo`.
