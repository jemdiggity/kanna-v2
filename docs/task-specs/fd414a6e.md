# Task fd414a6e: simultaneous agent-session exits

## Goal

Reconstruct the staging incident on desktop `desktop-aa43ab36` from 18:43:00–18:44:00 UTC on 2026-09-03, identify what terminated or interrupted tasks `97f63d2f`, `487e1036`, and `b6048ec2`, and classify the cause from primary logs and task events.

## Scope and constraints

- Read the exact staging instance's daemon, server, task-event, and frontend evidence; quote the decisive lines and preserve a timeline here.
- Keep all interaction with the running staging instance read-only. Do not restart it, kill sessions, or edit its configuration.
- If the cause is an input/lifecycle/server fan-out defect, fix the owning layer and add a regression test. If it is external or deliberate human action, make no product change and record a failure verdict of `no product defect found` with the cause.
- No UI changes. Do not guess if the relevant logs have rotated away.

## Done when

The minute is reconstructed and classified, and either the root-cause fix has regression coverage with `./kd test all` passing, or the no-defect cause is recorded with quoted evidence and a failure verdict.

## Incident timeline

The live instance was first resolved through `kanna_info`: staging at
`http://127.0.0.1:48121`, desktop
`desktop-aa43ab36-e634-4ae9-b629-e8c8a91f7bff`, version
`0.3.0-staging.9`. All reads below were against that instance. Checked files:

- `~/Library/Application Support/build.kanna.staging/Kanna/kanna-daemon_37964_2026-09-03_07-59-53.log`
- `~/Library/Application Support/build.kanna.staging/Kanna/kanna-daemon-lifecycle.log`
- `~/Library/Application Support/build.kanna.staging/Kanna/kanna-server.log`
  and its run log `kanna-server_5163_2026-09-03_07-38-20.log`
- `/private/tmp/kanna-webview-58c79e45.log`
- the read-only `/v1/task-events` feed and the provider transcript for task
  `5741da6e`'s Claude review session

Times are UTC, with the daemon's local MDT time in parentheses:

1. At `18:43:24.708`, task `5741da6e`'s Claude review agent stopped its own
   tracked background-task handle. Its transcript confirms:
   `Successfully stopped task: b9nrviz84 (... ./kd test all ...)`.
2. At `18:43:29.083`, that same agent issued this Bash tool command:
   `pkill -f "kd test all" 2>/dev/null; sleep 1; ...`. On this Mac, `pkill -f`
   matches a substring of the full argument list, sends `TERM` by default, and
   excludes its own ancestors. The command therefore spared the invoking
   `5741da6e` agent but matched unrelated provider argument lists containing
   the tasks' `./kd test all` completion text. Read-only task details confirm
   that `97f63d2f`, `487e1036`, and `b6048ec2` each contained that phrase;
   surviving PR task `2967f435` did not.
3. At `18:43:29.170538` (`12:43:29.170538`), the daemon first observed
   `[stream] eof session=97f63d2f`, followed by
   `[stream] exit session=97f63d2f code=0`. At `18:43:29.172283`, it observed
   `[stream] eof session=487e1036`, followed by
   `[stream] exit session=487e1036 code=0`. Their Codex TUIs printed
   `Conversation interrupted` while handling the termination.
4. At `18:43:29.225`, Claude recorded task `b6048ec2`'s background
   `./kd test all` command as `status: killed`. At `18:43:29.851806`, the
   daemon observed `[stream] eof session=b6048ec2`; at `18:43:29.858664` it
   recorded `[stream] exit session=b6048ec2 code=143`, the expected shell
   status for SIGTERM.
5. The durable event feed recorded the same order: seq `2795`/`2796` marked
   `97f63d2f` cancelled/exited with exit code 0, seq `2797`/`2798` did the same
   for `487e1036`, and seq `2799` marked `b6048ec2` failed with exit code 143.
6. Task `2967f435` remained alive and produced seq `2801` at `18:43:42`,
   `run.finished`/`succeeded`, summary
   `Created PR https://github.com/tampopogk/kanna/pull/1277`. The daemon lines
   at `18:43:42` are for its separate `td-task-2967f435-4` teardown session,
   not its live PR agent.
7. The manager resumed the three affected tasks after the minute boundary:
   event seq `2811` starts `97f63d2f` at `18:44:14`, seq `2815` starts
   `487e1036` at `18:44:33`, and seq `2816` starts `b6048ec2` at `18:44:34`.

## Classification

This is case (d), an external process: an unrelated agent deliberately ran an
over-broad host `pkill` while trying to restart only its own test command. It
was not a deliberate action by the owner and was not a Kanna product fan-out.

The daemon did not receive a signal, kill, handoff, adoption, input-delivery,
or PTY-allocation request for any affected session. There are no `[signal]`,
`[kill]`, `ENXIO`, or `[pty-exhaustion]` lines for them before their EOFs; the
first `[kill]` in the minute is the unrelated
`[kill] session=74b9056d` at `18:43:57.940166`. The lifecycle audit's last
entry for daemon PID 37964 is its startup authorization at `07:59:54` local,
so no lifecycle event occurred at 12:43. Existing PTYs are isolated with
`setsid()`, and an ENXIO path only rejects a new allocation.

The server was an observer, not the sender. Immediately before the exits its
run log contains only heartbeats and event reads, including
`GET /v1/task-events ... cursor=2794`; the next target-specific request is a
read, `GET /v1/tasks/97f63d2f/logs`, after the exit events. The three
`POST .../actions/resume` calls appear later, after their sessions were already
missing. There is no close, advance, rerun, transfer, or input POST for the
three targets in the incident interval.

The frontend was attached to task `f572a73d`, not an affected task: its last
selection was `selectItem {"from":"487e1036","to":"f572a73d"}` at
`12:38:39.083`. From `12:42:19.522` until the exits, the frontend log is quiet;
at `12:43:29.218` through `12:43:30.381` it only reports snapshot refreshes
after the server state changes. There is no focus change, relaunch, restart
action, terminal input, or bulk key event at 12:43.

Machine pressure and macOS termination are also refuted. The contemporaneous
diagnosis recorded 53.04--88.39% idle CPU, 13 GB unused out of 64 GB, memory
pressure 92% free, zero throttled pages, and zero swap-ins/outs. The desktop,
server, and daemon remained alive and heartbeating, and no diagnostic report
was created during 12:43:00--12:44:00. The exact 87 ms offset from the external
`pkill` invocation to the first EOF, plus the provider-specific exit handling,
identifies the sender directly.

## Result

No product defect found. Per the task's external-cause branch, no product code
or regression test is changed. The corrective lesson belongs to agent shell
practice: never use `pkill -f` with text that can occur in other agents'
command lines; stop the exact recorded child PID/process group instead.
