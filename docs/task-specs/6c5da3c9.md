# Task 6c5da3c9

## Goal

Make `kd` report truthfully in two places: `kd release status` must agree with the staging publish gate about release-branch freezes after a recorded reset, and `kd clean` must clean the Bazel output base selected by Bazel's effective startup configuration.

## Scope and constraints

- Change only `tools/kd` implementation and tests, plus this task spec.
- Reuse the release gate's lineage decision; do not introduce release-flow behavior changes.
- Resolve the Bazel output base by running Bazel, and fail explicitly when Bazel cannot run or does not return a usable path.
- Distinguish a valid resolved output base with nothing present from failure to resolve the correct location.

## Done

- Status reports an active freeze without reset authorization and a waived, inactive freeze when the recorded reset authorizes the next main publish.
- Clean targets the output base returned by Bazel and has test coverage for resolution and failure modes.
- `pnpm exec tsc --noEmit`, focused `tools/kd` tests, and `./kd test all` pass.
