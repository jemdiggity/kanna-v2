# Task b5b15c45

Fix PR #1170 on its existing `feat/mobile-remote-repo-checkout` branch so repo-scoped routing or checkout failures surface through `repoCommandStatus` and `repoCommandErrorMessage` without changing a healthy `connectionState` from `connected` to `error`.

Keep the existing #1169 regression test unchanged, add focused coverage for a machine-lacks-repo checkout/routing failure, run the mobile test suite and `./kd test all`, then commit and push the fix to the same branch. Do not revisit the rest of #1170, which the merge master already verified.
