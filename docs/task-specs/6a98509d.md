# Task 6a98509d — Anonymous push notifications, phase 4

## Goal

Complete the owner-approved final phase of anonymous push delivery: account-authenticated relay sessions also prove the desktop's anonymous identity, notification delivery unions account-bound and pair-bound FCM tokens with token-level deduplication, mobile preserves anonymous registration through sign-in/sign-out transitions, and notifications are free outside the `cloud_relay` entitlement.

## Scope and constraints

- Implement the Task 4 acceptance criteria in `docs/specs/anonymous-push-notifications.md`: mixed identity delivery, upgrade-in-place mobile registration, the notification entitlement carve-out, and the Decision 5 amendment in `docs/specs/accounts-and-billing.md`.
- Add mixed-state exact-once, entitlement-enforcement-on, and mobile sign-in/sign-out transition tests.
- Keep tunnels, invoke, snapshots, and remote terminal account- and entitlement-gated; anonymous principals remain notification-only.
- Use staging cloud only. Do not deploy production or enable the production entitlement flag day; report readiness for those human-gated actions.
- Verify signed-out paired-device push in foreground, background, and terminated iOS states when a physical paired staging device is available; otherwise document the concrete verification blocker without claiming success.

## Done when

The required code, specs, and automated tests pass; feasible staging/device verification is recorded; and production-gated follow-up readiness is stated without performing it.

## Verification record

- Revision round 1 added a kanna-server↔relay WebSocket regression test for the signed-in desktop/anonymous-pairing handshake. The fake relay receives account credentials plus `anon_pub_key`, challenges and verifies the anonymous proof on both the account probe and owning connection, returns one `auth_ok` with notification, desktop-routing, and task-snapshot capabilities, and confirms the real relay loop enables routing, publishes a schema-v2 snapshot, and successfully publishes a notification. This covers the prior defect where `connect_to_relay` consumed the owning connection's only `auth_ok` before capability initialization.
- Revision round 1 verification passed: focused `cargo test -p kanna-server relay::tests::dual_identity_auth_initializes_capabilities_and_publishes_notification -- --nocapture`; relay protocol integration (`pnpm --filter kanna-relay test -- test/integration.test.ts`, 271 tests); `cargo fmt --all`; `cargo clippy --all-targets --all-features` (pre-existing warnings only); and the canonical `./kd test all` repository gate. The first full-gate attempt exhausted the worktree build volume while linking; after cleaning only this worktree's Cargo target, the exact gate passed on retry.
- Automated coverage passed for mixed account/anonymous delivery, exact-once token deduplication, entitlement-on free push with paid paths denied, mobile sign-in/sign-out transitions, and desktop dual-identity authentication. The repository-wide gate completed 702/703 tests successfully on its first run; its sole failure was an `ENOTEMPTY` cleanup race in the unrelated `kd` clean-clone test, which passed immediately when rerun in isolation.
- Staging relay health reported commit `d29433be6b7a`; Cloud Build `64c37090-ab8e-4d94-8c36-902a2526a35e` completed successfully.
- Staging mobile OTA update `b22d7d76-3d50-7e46-0a04-398b902a1a78` points runtime `2.2.2` at commit `d29433be6b7a`.
- Physical APNs delivery passed on the signed-out iPhone 15 Pro in foreground, background, and killed states; the detailed evidence is recorded below.
- No production deployment or entitlement flag-day action was performed.

### Physical-device follow-up, 2026-08-26

The task manager relayed new owner evidence on 2026-08-26: the owner received zero pushes from four earlier staging `kanna_notify_mobile` calls even though each returned `acceptedCount: 1`, and this task's later foreground attempts returned `noRegisteredDevices`. The manager asked this task to verify the hypothesis that FCM rejected a stale token and phase 3's invalid-token reconciliation retired the binding, then obtain a fresh signed-out registration and test foreground, background, and terminated states with distinctly labeled pushes. Production deployment and the entitlement flag day remain excluded without a new human instruction.

The surviving staging evidence proves that the usable registration disappeared, but does not uniquely prove the proposed cause: the phase-4 relay replacement removed the old container logs containing the morning FCM responses; the current container records an account-token registration; and Firestore currently contains neither an active account push-device row nor an anonymous pairing watermark. A 48-second watch after requesting a signed-out force-quit/relaunch remained at zero account and anonymous registrations. The invalid-token path is consistent with the accepted-then-empty sequence, but is not recorded as confirmed without a retained provider response or reconciliation record.

The owner changed the physical verification target on 2026-08-26 from the iPhone 15 to the iPhone XR. The required state matrix is unchanged: with the staging app signed out and a freshly confirmed anonymous binding, send separately labeled foreground, background, and terminated-state pushes and record the owner's receipt confirmation for each on the XR. Results from the iPhone 15 do not satisfy this verification.

The owner then clarified on 2026-08-26 that Kanna Staging was not installed on the XR and proposed this worktree's Kanna Dev build pointed at staging. Preflight confirmed `build.kanna.app.dev` was installed, but its provisioning profile omitted the XR; more importantly, the mobile notification bootstrap deliberately disables registration for `appEnv === "dev"`, so that profile cannot verify APNs. The corrected test artifact is `./kd mobile run --device --staging --install`: a staging-identity Release build made from this worktree, using staging Firebase/relay and automatic device provisioning. This replaces both the earlier OTA-relaunch instruction and the proposed Dev-on-staging profile for this XR push test.

On 2026-08-27 the owner reversed the device choice: "switch back to the iPhone 15 for the signed-out push verification (not the XR)." The owner will launch Kanna Staging once to download the OTA and relaunch it to apply the update, while remaining signed out. The binding that was reconciled away on 2026-08-26 must be replaced by a newly observed anonymous registration before testing. The required matrix is now foreground, background, and terminated-state delivery on the iPhone 15, with a distinct label and owner receipt confirmation for each. If the double relaunch creates no binding, verify the installed native runtime and report plainly if it predates runtime `2.2.2`. The prior XR-only requirement is superseded; the staging-only and production/flag-day exclusions remain unchanged.

The 2026-08-27 double-relaunch watch observed zero anonymous binding documents for three minutes. Direct inspection of the attached iPhone 15's Expo update database proves that its installed staging app is on runtime `2.2.2`; the current update has three successful launches and zero failed launches. Its signed-out persisted state contains one trusted desktop (`desktop-21b320e8-a5ad-4fae-9d87-1db14090f0a9`, installed staging on LAN port `48121`) with a paired-device secret but no anonymous push identity or certificate. That installed staging server returns `404` for the certificate-reissue route, so relaunching cannot upgrade this legacy pairing. Physical verification therefore requires pairing Kanna Staging on the iPhone 15 to the current worktree server before the fresh binding and state matrix can be exercised.

After the owner paired the signed-out iPhone 15 Pro to the current worktree server, staging held one active anonymous binding for that desktop/device. On 2026-08-27 the owner explicitly confirmed receipt in all three states:

| App state | Push title | Relay result | Owner-observed result |
|---|---|---|---|
| Foreground | `verify: foreground` | `acceptedCount: 1`, `failedCount: 0` | Received |
| Background | `verify: background` | `acceptedCount: 1`, `failedCount: 0` | Received |
| Killed (swiped away) | `verify: killed` | `acceptedCount: 1`, `failedCount: 0` | Received |

The phone remained signed out. Its staging app ran OTA source `b22d7d76-3d50-7e46-0a04-398b902a1a78` on native runtime `2.2.2`. Delivery used staging cloud and this branch's worktree server (`desktop-4e4efa47-ce11-49d1-a377-289c7a6eb176`, `http://172.16.0.54:48154`, environment `development`, server version `0.0.68`) with the phase-4 anonymous delivery path. The code, automated coverage, staging relay/OTA deployment, fresh anonymous registration, and physical APNs matrix are ready for the separately human-gated production deployment and entitlement flag day. Neither gated action was performed.
